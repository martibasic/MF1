Zadatak 97 – Pražnjenje spremnika: Torricelli i vrijeme istjecanja
Prompt:

Create an interactive HTML/JS/CSS widget simulating a cylindrical tank draining through a small orifice at the bottom. Draw the tank as a rectangle with an animated blue water fill level h(t) that drops in real time. Show the exit jet as animated particles leaving the orifice horizontally. Add sliders for: tank diameter D (0.2–1.0 m), orifice diameter d (0.01–0.05 m), initial water height h0 (0.1–2.0 m), discharge coefficient Cd (0.6–1.0). Animate the water level dropping according to h(t) = (√h0 - (d²·Cd·√(2g))/(2D²)·t)² — the level drops faster at first (high h = high velocity) and slower near empty. Show a real-time graph on the right: x-axis = time, y-axis = water level h(t) as a blue curve — it is NOT linear but follows a square-root decay. Show simultaneously: exit velocity v(t) = Cd·√(2gh) as a green curve on the same graph, decaying with h. Display the drain time T = 2D²·√h0/(d²·Cd·√(2g)) updating live. Add a "Play/Pause" button and a "Reset" button. Show the instantaneous flow rate Q(t) = Cd·(πd²/4)·√(2gh) as an animated bar. Include a "Why is it not linear?" explanation panel: as h drops, v drops, so Q drops, so the tank empties ever more slowly. Show the Torricelli derivation steps animated.


Zadatak 98 – Dva mlaza iz spremnika: parabolični letovi
Prompt:

Create an interactive HTML/JS/CSS widget showing two horizontal jets exiting from a tank wall at different heights, both landing at the same horizontal distance L. Draw the tank on the left with two orifices: jet 1 from upper orifice at depth h0 below surface, jet 2 from lower orifice at depth h0+h below surface. Both jets follow parabolic trajectories to the right. Add sliders for: tank total height H (3–10 m), water depth above upper orifice h0 (1–5 m), horizontal distance L (5–20 m). Animate both jets as streams of blue particles following parabolas: x = v0·t, z = ½g·t². Show that jet 1 (faster, exits higher) and jet 2 (slower, exits lower) can land at the same point — the faster jet travels farther horizontally but also falls more. Mark the landing point with a red dot. Display the condition for equal landing: H = L²/(4·h0) and h = 2.25 m (for default values). Add a "sweep h" animation: slowly change h and show both landing points moving — highlight the moment they coincide. Show velocity vectors at exit for both jets. Include a symmetry insight: the two solutions are symmetric about h0 = H/2, just like the range formula x = 2·√(h·(H-h)).


Zadatak 99 – Rotirajući spremnik: paraboloid i Bernoulli
Prompt:

Create an interactive HTML/JS/CSS widget simulating a rotating tank with fluid forming a paraboloid free surface, with a pipe attached extracting flow. Draw the tank cross-section with the free surface shown as a parabola — flat when ω=0, increasingly curved as ω increases. The fluid surface drops at the center (paraboloid apex) and rises at the walls. Add sliders for: angular velocity ω (0–8 rad/s), tank radius R (0.5–2.0 m), pipe radius r (0.5–1.5 m, the radial position of the pipe), pipe length L (1–4 m), height H1, gauge pressure at apex pm0 (10000–80000 Pa). Animate: (1) the paraboloid surface curving more steeply as ω increases — draw as a smooth parabola, (2) the apex dropping by Δz = ω²r²/4g shown with a labeled arrow, (3) flow particles moving through the attached pipe. Show the modified Bernoulli equation for rotating flow: p/ρg + z + (w² - ω²r²)/2g = const with the centrifugal term highlighted. Display pipe flow velocity w = √(2pm0/ρ - 2g(L+H1+ω²r²/4g)) and V̇ = w·A updating live. Show the pressure at point A. Color the rotating fluid with a centrifugal pressure gradient: darker blue at larger radius (higher pressure due to rotation).


Zadatak 100 – Konvergentno koljeno: sila i zakon količine gibanja
Prompt:

Create an interactive HTML/JS/CSS widget for a converging pipe bend (elbow) redirecting flow from horizontal to angle α upward, applying the momentum equation to find the reaction force. Draw the elbow as a smooth curved pipe: wide horizontal inlet (section 1, area A1) curving upward to narrow exit (section 2, area A2, angle α from horizontal). Add sliders for: mass flow rate ṁ (5–25 kg/s), inlet area A1 (0.005–0.02 m²), outlet area A2 (0.0003–0.002 m²), bend angle α (0–90°), height difference h (0–0.5 m), momentum correction factor βk (1.0–1.1). Compute: v1 = ṁ/(ρ·A1), v2 = ṁ/(ρ·A2), then p1 from Bernoulli. Show the momentum balance as animated arrows: inlet momentum flux ṁ·v1 (horizontal, blue arrow pointing right), outlet momentum flux ṁ·v2 decomposed into x and z components (green arrows), reaction force FR with components FRx and FRz (red arrows). The momentum equation: FR = ṁ·v2·(direction vector) - ṁ·v1·(inlet direction) + pressure forces. Animate: as α changes, the outlet arrow rotates and the reaction force arrow rotates and scales. Show the vector diagram (triangle of momentum fluxes). Display FRx, FRz, |FR|, and angle of FR updating live. Add a "force decomposition" toggle.


Zadatak 101 – Koljeno 180°: U-zavoj i sila
Prompt:

Create an interactive HTML/JS/CSS widget for a 180° U-bend pipe where flow enters horizontally from the right and exits horizontally to the left (completely reversed direction). Draw the U-bend with inlet section 1 (wide, right side) and outlet section 2 (narrow, left side), with height difference h between them. Add sliders for: mass flow rate ṁ (5–25 kg/s), A1 (0.005–0.02 m²), A2 (0.0003–0.002 m²), h (0–0.5 m), βk (1.0–1.1). Animate flow particles entering from the right, curving through the U-bend, and exiting to the left. Show momentum flux arrows: inlet ṁ·v1 pointing RIGHT (positive x), outlet ṁ·v2 pointing LEFT (negative x) — both arrows point in OPPOSITE directions, so the reaction force is the SUM of both magnitudes in the x-direction. Highlight the key physics: a 180° bend creates the LARGEST possible reaction force for a given flow rate because inlet and outlet momenta ADD rather than partially cancel. Show FRx = βk·ṁ·(v1 + v2) + p1m·A1 updating live. Compare with the 30° bend from task 100 at the same flow rate — show FR(180°) >> FR(30°). Display the force vector diagram. Add a "bend angle sweep" animation from 0° to 180° showing how FR changes.


Zadatak 102 – Mlaz na ploču: sila udara
Prompt:

Create an interactive HTML/JS/CSS widget for a water jet impacting a stationary flat plate perpendicular to the flow. Draw the nozzle on the left shooting a horizontal jet (blue cylinder of flow) that hits the vertical plate on the right. After impact, water spreads radially outward along the plate surface in all directions (shown as a symmetric fan of thin streams). Add sliders for: jet velocity v (5–40 m/s), mass flow rate ṁ (2–20 kg/s), nozzle diameter d (computed from ṁ and v), plate position (distance from nozzle, cosmetic only). Animate: (1) a stream of blue particles flying from nozzle to plate, (2) upon impact, particles split and flow radially outward along the plate in 8 directions as thin streams, (3) the reaction force arrow FR pointing LEFT (pushing the plate) with magnitude = ṁ·v, scaling with the slider. Show the momentum equation: inlet x-momentum = ṁ·v, outlet x-momentum = 0 (all flow is now perpendicular to x), so FR = ṁ·v. Display FR = ṁ·v = ρ·A·v² updating live. Add a "moving plate" mode: when the plate moves away at velocity u, FR = ρ·A·(v-u)² — show how the force drops as the plate retreats. Show power transferred to moving plate P = FR·u with an optimal u for maximum power.


Zadatak 103 – Vjetroturbina: Betzov zakon i učinkovitost
Prompt:

Create an interactive HTML/JS/CSS widget for a wind turbine, demonstrating the Betz limit and the momentum theory of wind turbines. Draw a side-view cross-section of the wind turbine: show the streamtube (dashed boundary lines) that widens after the turbine as the wind slows down. The turbine is represented as a disc in the center. Show four labeled cross-sections: 1 (far upstream, v1 = wind speed, narrow streamtube), 3 (just upstream of turbine, v3), 4 (just downstream of turbine, v4 = v3), 2 (far downstream, v2 < v1, wide streamtube). Add sliders for: wind speed v1 (1–15 m/s), turbine diameter D (5–20 m), air density ρ (1.0–1.4 kg/m³), efficiency η (0–0.593, the Betz limit). Animate: streamlines expanding downstream as wind slows, particle speed decreasing after the turbine. Show the power extracted P = η·½·ρ·A·v1³ as a bar. Show available wind power P_wind = ½·ρ·A·v1³. Display efficiency η = P/P_wind. Draw a live graph: power vs wind speed (cubic curve). Show the Betz limit line at η = 16/27 = 59.3% — highlight that no turbine can exceed this. Show the thrust force FR = ṁ·(v1-v2) = ρ·A·v3·(v1-v2) for both v1 and 2·v1, demonstrating FR scales with v². Add a "double wind speed" button showing power increases 8x but force only 4x.

Widget A – Zakon količine gibanja: vizualni dokaz
Prompt:

Create an interactive HTML/JS/CSS widget that visually proves Newton's second law applied to fluid flow — the momentum equation ΣF = d(mv)/dt = ṁ·(v_out - v_in). Draw a generic control volume (dashed rectangle) with one inlet (left) and one outlet (right, at any angle). Show animated flow particles entering and leaving. At each boundary, draw momentum flux arrows: thick blue arrow at inlet showing ṁ·v1 (pointing inward), thick red arrow at outlet showing ṁ·v2 (pointing outward in exit direction). Show all external forces on the control volume as green arrows: pressure force at inlet p1·A1 (pushing right), pressure force at outlet p2·A2 (pushing left, in outlet direction), and reaction force FR (unknown, shown as dashed). The momentum balance panel shows: FR + p1·A1·(direction1) - p2·A2·(direction2) = ṁ·v2·(direction2) - ṁ·v1·(direction1). Add sliders for outlet angle θ (0–180°), v1, v2, p1, p2, A1, A2. As θ changes, animate the outlet momentum arrow rotating and the reaction force FR arrow updating direction and magnitude automatically. Show the x and z components separately. Include a vector addition diagram (parallelogram rule) showing how inlet and outlet momentum flux vectors combine to give FR. Color code: blue = momentum in, red = momentum out, green = forces.


Widget B – Pražnjenje kroz bočni otvor: usporedba visina
Prompt:

Create an interactive HTML/JS/CSS widget showing a tank with a side orifice at height h_hole above the bottom, with water level H above the bottom. The jet exits horizontally and follows a parabolic path, landing at horizontal distance x from the tank wall. Draw the tank, the parabolic jet, and the landing point. Add sliders for: tank height H_tank (1–4 m), water level H (0.2·H_tank to H_tank), orifice height h_hole (0 to H-0.1), orifice diameter d (5–30 mm). Compute: exit velocity v = √(2g·(H-h_hole)), fall height = h_hole, flight time t = √(2·h_hole/g), landing distance x = v·t = 2·√(h_hole·(H-h_hole)). Animate the parabolic jet with moving particles. Show a live curve on the right panel: x vs h_hole/H for the current H — it is a smooth arch peaking at h_hole = H/2. Draw a movable vertical line on this graph that the user can drag, showing the current orifice position and its landing distance. Highlight the maximum range point (h_hole = H/2) with a star. Add a "show all orifices" mode: simultaneously show 7 jets from orifices at h/H = 0.1, 0.2, 0.3, 0.5, 0.7, 0.8, 0.9 — all colored differently, landing at different distances, visually confirming the symmetry x(h) = x(H-h). Show the drain time if the orifice were used to drain the tank.


Widget C – Mlaz na zakrivljenu lopaticu: sila i smjer
Prompt:

Create an interactive HTML/JS/CSS widget for a water jet impacting a curved vane (bucket/blade), which deflects the jet through angle β. Draw the curved vane as a smooth arc. The jet enters horizontally from the left and exits at angle β from horizontal (user-controllable). Add sliders for: jet velocity v (5–30 m/s), mass flow rate ṁ (1–15 kg/s), deflection angle β (0–180°), vane velocity u (0–v, for moving vane mode). For STATIONARY vane: animate particles entering horizontally, curving along the vane surface, and exiting at angle β. Show momentum flux vectors: inlet ṁ·v horizontal (blue), outlet ṁ·v at angle β (red). Compute FRx = ṁ·v·(1-cosβ) and FRz = ṁ·v·sinβ. Show |FR| = ṁ·v·√(2(1-cosβ)) updating live. For MOVING vane mode: the relative velocity is (v-u), so FR = ṁ·(v-u)·(1-cosβ) — show this dropping as u→v. Draw the vector triangle of forces. Add a live polar plot: FR magnitude vs deflection angle β (0–180°) — it peaks at β=180° (180° reversal gives maximum force). Show a "special cases" panel: β=0° (no deflection, FR=0), β=90° (right angle turn), β=180° (complete reversal, maximum FR). Animate the vane rotating as β slider changes.


Widget D – Mlazni pogon: reakcijska sila i Newtonov treći zakon
Prompt:

Create an interactive HTML/JS/CSS widget demonstrating jet propulsion as a direct consequence of Newton's third law applied to fluid momentum. Draw a boat or rocket shape (user can toggle between: boat with water jet, rocket with gas jet, garden sprinkler). For the BOAT mode: water is drawn in from the front (or sides) and ejected backward through a nozzle. Show inlet flow (slow, large area) and outlet jet (fast, small area). The momentum difference creates thrust. Add sliders for: inlet velocity v_in (0–3 m/s), outlet velocity v_out (5–30 m/s), nozzle area A_out (0.001–0.01 m²), fluid density ρ. Animate: particles entering slowly at the bow, accelerating through the internal pump (show pump as a spinning impeller), and exiting fast at the stern as a high-speed jet. Show thrust force F_thrust = ṁ·(v_out - v_in) as a forward arrow on the boat, and the equal/opposite jet momentum as a backward arrow on the water. For SPRINKLER mode: draw a rotating sprinkler with 3 arms, each with a nozzle at the end. Animate water flowing out tangentially — the reaction torque makes it spin. Show angular velocity ω increasing with flow rate. Include a momentum budget panel showing all terms. Animate the vehicle accelerating (position changing) when thrust exceeds drag.


Widget E – Sila na cijevni priključak: kombinirani Bernoulli i količina gibanja
Prompt:

Create an interactive HTML/JS/CSS widget that systematically walks through the two-step solution process for pipe fittings: (1) use Bernoulli to find pressures, then (2) use the momentum equation to find forces. Draw a pipe with a fitting that the user selects from a dropdown: 90° elbow, 180° U-bend, T-junction (flow splitting), converging nozzle, or sudden expansion. For each fitting, show inlet(s) and outlet(s) with labeled cross-sections. Add sliders for: flow rate V̇ (0.005–0.1 m³/s), inlet diameter D1, outlet diameter D2, fluid density ρ. STEP 1 panel (Bernoulli): show the calculation chain — from V̇ compute v1=V̇/A1, v2=V̇/A2, then apply Bernoulli to get p2 from p1. Display each step as it computes. STEP 2 panel (Momentum): show the control volume with all forces: p1·A1 (inlet pressure force), p2·A2 (outlet pressure force), ṁ·v1 (inlet momentum flux), ṁ·v2 (outlet momentum flux), and FR (unknown). Solve for FR with animated vector addition. Show FR decomposed into FRx and FRz with arrows on the fitting diagram. When the user changes the fitting type, all geometry, arrows, and calculations update automatically. Include a "bolt stress" indicator: what force must the pipe bolts withstand?


Widget F – Nestacionarno pražnjenje: dva spojena spremnika
Prompt:

Create an interactive HTML/JS/CSS widget simulating two tanks connected by a pipe at the bottom, with fluid transferring from the higher-level tank to the lower-level tank until levels equalize. Draw two cylindrical tanks side by side connected at the bottom by a pipe with valve. Initially: left tank has level H1, right tank has level H2 < H1. Add sliders for: left tank diameter D1 (0.3–1.5 m), right tank diameter D2 (0.3–1.5 m), connecting pipe diameter d (0.02–0.1 m), initial H1 (0.5–3 m), initial H2 (0–H1-0.1 m), discharge coefficient Cd (0.6–1.0). Animate: the water level in the left tank dropping and the right tank rising simultaneously. The flow rate Q(t) = Cd·(πd²/4)·√(2g·(H1(t)-H2(t))) — as levels equalize, Q drops to zero. Show a live graph: both water levels as functions of time converging to the equilibrium level H_eq = (D1²·H1 + D2²·H2)/(D1²+D2²) (conservation of volume). Show Q(t) as a third curve dropping to zero. Display time to reach 99% equalization. Add a "equal tanks" preset (D1=D2) and "unequal tanks" preset. Show the flow velocity in the connecting pipe as an animated speed indicator. Highlight the physics: this is analogous to capacitor discharge — exponential-like decay, not linear.


Widget G – Betzov limit: maksimalna snaga iz vjetra
Prompt:

Create an interactive HTML/JS/CSS widget dedicated to deriving and visualizing the Betz limit (maximum theoretical wind turbine efficiency = 16/27 ≈ 59.3%). Draw a wind turbine streamtube: upstream (v1, narrow tube), at turbine disc (v_disc = (v1+v2)/2, medium tube), downstream (v2, wide tube). Add sliders for: upstream wind speed v1 (2–20 m/s), speed ratio a = v2/v1 (0–1, the axial induction factor), turbine diameter D (5–30 m), air density ρ. Compute: v_disc = v1·(1+a)/2, thrust T = ½·ρ·A·(v1²-v2²), power P = T·v_disc = ½·ρ·A·(v1²-v2²)·(v1+v2)/2, power coefficient Cp = P/(½·ρ·A·v1³). Show a large live graph: Cp vs a (speed ratio v2/v1 from 0 to 1). The curve is a parabola peaking at a = 1/3 (v2 = v1/3) giving Cp_max = 16/27. Draw a movable vertical line that the user drags — as they move it, Cp rises then falls, with the maximum clearly marked at a=1/3. Animate the streamtube: when a=0 (all wind stopped), v2=0, tube massively wide — no flow, no power. When a=1 (no slowdown), v2=v1, tube doesn't expand — no force, no power. The optimum is in between. Show actual wind turbine efficiencies as horizontal reference lines: typical = 35–45%, theoretical max = 59.3%. Display P in kW and thrust F_R updating live.


Widget H – Zakon količine gibanja za rotacijske strojeve: moment sile
Prompt:

Create an interactive HTML/JS/CSS widget demonstrating the angular momentum equation for rotating machinery — the Euler turbomachine equation: T = ṁ·(r2·vθ2 - r1·vθ1), where vθ is the tangential velocity component. Draw a centrifugal pump or turbine in cross-section: an impeller with inlet radius r1 and outlet radius r2, rotating at angular velocity ω. Show velocity triangles at inlet and outlet: absolute velocity v, blade velocity u=ω·r, and relative velocity w = v - u — drawn as a triangle with animated components. Add sliders for: rotation speed N (100–3000 rpm → ω = 2πN/60), inlet radius r1 (0.05–0.2 m), outlet radius r2 (0.1–0.4 m), mass flow rate ṁ (1–20 kg/s), blade angles β1 and β2 (15–75°). Compute tangential components vθ1 and vθ2 from the velocity triangles. Show torque T = ṁ·(r2·vθ2 - r1·vθ1) and power P = T·ω = ṁ·(u2·vθ2 - u1·vθ1) (Euler equation). Animate the impeller rotating with particles following the blade passages. Show velocity triangles updating smoothly as sliders change. For PUMP mode: power input → fluid gains energy. For TURBINE mode: fluid loses energy → power output. Toggle between pump and turbine. Show head H = P/(ṁ·g) = (u2·vθ2 - u1·vθ1)/g updating live.