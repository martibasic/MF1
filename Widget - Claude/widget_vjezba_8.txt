Zadatak 70 – Difuzor: kontinuitet i promjena brzine
Prompt:

Create an interactive HTML/JS/CSS widget simulating fluid flow through a diverging diffuser (pipe that expands from diameter D1 to D2). Draw the diffuser as a 2D cross-section with smooth expanding walls. Animate flow using moving streamlines (horizontal animated dashed lines) that spread apart as the pipe expands — more spread = slower flow. Add sliders for: inlet diameter D1 (50–150 mm), outlet diameter D2 (100–250 mm, always > D1), outlet velocity v2 (5–30 m/s), and fluid density ρ (800–1200 kg/m³). Display animated velocity arrows at inlet and outlet cross-sections whose lengths are proportional to velocity — the inlet arrow must always be longer (faster). Show a live color gradient inside the diffuser: dark blue at inlet (fast, high kinetic energy) transitioning to light blue at outlet (slow). Display the continuity equation A1·v1 = A2·v2 with all values substituted numerically and updating live. Show a results panel: v1_sr, volumetric flow rate V̇ = A1·v1, and mass flow rate ṁ = ρ·V̇. Add a "flow particle" animation: colored dots travel from left to right, moving faster at the narrow inlet and slowing at the wide outlet. Show cross-section area circles (to scale) at inlet and outlet for visual comparison.


Zadatak 71 – Miješanje fluida: voda + ulje → mješavina
Prompt:

Create an interactive HTML/JS/CSS widget for a mixing tank with two inlets (A = water from bottom, B = oil from left) and one outlet (C = mixture to the right), plus a drain D at the bottom. Draw the mixing chamber as a rectangle with labeled pipes. Animate three separate colored flows: inlet A (blue, water, from bottom), inlet B (yellow-brown, oil, from left), outlet C (turquoise, mixture, to right). Use animated moving dots in each pipe colored according to the fluid. Add sliders for: volumetric flow rate V̇_A (50–300 dm³/s), volumetric flow rate V̇_B (10–80 dm³/s), outlet diameter D_C (15–50 cm), water density ρ_A (950–1050 kg/m³), oil relative density ρ̄_B (0.6–0.95). Inside the mixing chamber, animate a swirling blend where the blue and yellow-brown colors mix into the turquoise output color — the blend ratio changes with slider values. Show the mixture density formula: ρ_C = (ṁ_A + ṁ_B)/(V̇_A + V̇_B) with live values. Display outlet velocity v_Csr = (V̇_A + V̇_B)/A_C. Show a live density color scale on the side. Display mass conservation: ṁ_in = ṁ_A + ṁ_B = ṁ_out with all numbers.


Zadatak 72 – Cilindrični spremnik: punjenje, pražnjenje, razina i zrak
Prompt:

Create an interactive HTML/JS/CSS widget simulating a cylindrical tank with four pipes: pipe 1 (inlet, left side), pipe 2 (outlet, bottom), pipe 3 (outlet, right side), pipe 4 (air vent, top). Draw the tank cross-section with the water level h shown as an animated blue fill that rises or falls in real time. Add sliders for: v1 (inlet velocity, 0–15 m/s), v2 (outlet velocity, 0–5 m/s), v3 (outlet velocity, 0–8 m/s), tank diameter d_s (0.3–1.0 m), pipe diameters D1, D2, D3, D4. Animate flow arrows in each pipe: right-pointing for inlet, left/down-pointing for outlets, with arrow thickness proportional to volumetric flow rate. Show the water level rising (blue fill animating upward) when Q_in > Q_out, and falling when Q_out > Q_in. Display dh/dt = (A1·v1 - A2·v2 - A3·v3)/A_s updating live with its sign (+ = rising, - = falling). For pipe 4 (air vent at top): show animated air flow arrows (grey dots) entering or leaving through pipe 4 — as water rises, air is expelled; as water falls, air enters. Display v4_sr = A_s·(dh/dt)/A4 with direction indicator. Show a flow balance bar chart: Q_in bar vs Q_out bar.


Zadatak 73 – Spremnik s vodom, benzinom i zrakom: maseni tok
Prompt:

Create an interactive HTML/JS/CSS widget for a stratified tank containing three fluid layers from bottom to top: water (blue), gasoline/benzin (yellow), and air (light grey). Show three pipes: pipe 1 (water inlet, bottom-right, pointing right INTO tank), pipe 2 (gasoline outlet, middle-left, pointing left OUT of tank), pipe 3 (air pipe, top, vertical). Animate each fluid layer as a distinct colored region with animated moving particles. The layer thicknesses change over time based on mass flow balance. Add sliders for: water mass flow in ṁ1 (5–20 kg/s), gasoline mass flow out ṁ2 (2–10 kg/s), gasoline relative density ρ̄_b (0.6–0.8), air density ρ_z (1.0–1.4 kg/m³), air pipe diameter d3. Apply mass continuity: ṁ_air = ṁ1 - ṁ2 (difference must be balanced by air). Show the direction of air flow in pipe 3 automatically reversing (arrow flips) when ṁ1 > ṁ2 vs ṁ1 < ṁ2. Display a mass balance equation panel: Σṁ_in = Σṁ_out with sign convention, and the resulting v3_air with direction label. Show volumetric flow rates for each fluid. Animate particles moving in correct directions.


Zadatak 74 – Aerotunnel s perforiranom stjenkom: odsisavanje
Prompt:

Create an interactive HTML/JS/CSS widget simulating a wind tunnel test section with perforated walls. Draw the tunnel cross-section from left to right: fan (left, at diameter D_f), diffuser section expanding from D_f to D_0, then the test section (diameter D_1 = D_2, length L) with perforated walls shown as a dashed boundary with small holes, then a contraction back to D_0. Show the main flow going LEFT (from right inlet v0 through the test section to outlet v2 then to fan v_f). Animate: (1) main flow streamlines moving left through the tunnel, (2) small suction arrows pointing OUTWARD through each perforation hole in the test section walls, (3) the boundary layer near the wall shown as a thin gradient region that gets thinner due to suction. Add sliders for: main inlet velocity v1 (20–60 m/s), perforation velocity v_p (5–20 m/s), number of perforations per m² n_p (400–1200), perforation diameter d_p (3–12 mm), test section length L (2–6 m), diameters D1, D_f. Display live: total suction area = n_p · π·d_p²/4 · L · π·D1, total suction flow V̇_suction, v2 = v1 - suction/A1, and fan velocity v_f from continuity at D_f. Show the boundary layer thickness visually decreasing along the test section length as suction increases.


Zadatak 75 – Mlazna pumpa: miješanje dvaju struja u cijevi
Prompt:

Create an interactive HTML/JS/CSS widget simulating a jet pump where a high-velocity central jet (v1, inner pipe D1) is surrounded by a slower annular flow (v2, annular region between D1 and D2). The two flows mix downstream and reach a uniform velocity v3 at section 3. Draw three regions left to right: (1) INLET: two concentric circles showing inner jet (dark blue, fast) and outer annular flow (light blue, slow), (2) MIXING ZONE: animated turbulent swirling where the two colors blend together with eddies, (3) FULLY MIXED: uniform single color at velocity v3. Animate flow particles: inner jet particles (dark blue dots) move much faster than outer particles (light blue dots). In the mixing zone, show the dots gradually equalizing speed. Add sliders for: v1 (10–40 m/s), v2 (1–10 m/s), inner diameter D1 (50–150 mm), outer diameter D2 (150–400 mm). Apply continuity: v3 = (A1·v1 + A_annular·v2) / A2 where A_annular = π/4·(D2²-D1²). Show v3 updating live with an arrow at section 3. Display a velocity profile diagram: at section 1 show a two-step profile (v1 center, v2 outer), at section 3 show a flat uniform profile at v3. Show the area ratio A1/A2 and momentum mixing physics explanation panel.

Naravno! Evo dodatnih widgeta temeljenih na fizikalnim principima Vježbe 8 – Jednadžba kontinuiteta:

Widget A – Interaktivna vizualizacija jednadžbe kontinuiteta: A·v = const
Prompt:

Create an interactive HTML/JS/CSS widget that visually demonstrates the continuity equation A·v = const for incompressible flow. Draw a pipe with a smoothly varying cross-section — the user can drag control points on the pipe walls to reshape it (make it narrower or wider at any point). As the shape changes, animate: (1) streamlines that pack closer together where the pipe is narrow (higher velocity) and spread apart where wide (lower velocity), (2) velocity arrows at multiple cross-sections along the pipe whose lengths are strictly proportional to 1/A at that point, (3) a color gradient from dark blue (fast, narrow) to light blue (slow, wide). Show a live graph below the pipe: x-axis = position along pipe, y-axis = both cross-sectional area A(x) (red curve) and velocity v(x) (blue curve) — they must be mirror images of each other (when A doubles, v halves). Add a slider for inlet velocity v0 and inlet diameter D0. Show Q = A·v displayed at 5 evenly spaced cross-sections, all showing the same constant value. Add animated flow particles (dots) that speed up in narrow regions and slow down in wide regions. Include a "pulse" button that sends a colored blob through the pipe, visibly accelerating and decelerating.


Widget B – Strujnice i brzinsko polje: što je strujnica?
Prompt:

Create an interactive HTML/JS/CSS widget that teaches students what streamlines are and how they relate to velocity fields. Draw a 2D flow field (rectangular domain) with an obstacle in the middle (circle or rectangle, toggleable). Compute and animate streamlines as smooth curves tangent to the local velocity field everywhere. Show velocity vectors as small arrows at a grid of points — arrow length and color proportional to local speed. The user can click anywhere in the flow field to release a flow particle (colored dot) that travels along the local velocity field in real time, tracing out a streamline. Add controls: (1) obstacle shape toggle (none / circle / rectangle / airfoil), (2) flow speed slider (overall magnitude), (3) "release particle" mode where clicking drops a tracer, (4) "show all streamlines" button. Show that streamlines never cross (add a visual warning/flash if two tracers get too close). Highlight: where streamlines are close together = high velocity (dark blue), where spread apart = low velocity (light blue). Add a "Continuity check" panel: pick two cross-sections with a draggable ruler, show A1·v1_avg = A2·v2_avg confirmed numerically. Include a short animated explanation text that fades in: "Fluid cannot be created or destroyed — what flows in must flow out."


Widget C – Kontrolni volumen: vizualizacija zakona očuvanja mase
Prompt:

Create an interactive HTML/JS/CSS widget that makes the control volume concept for mass conservation tangible. Draw an arbitrary closed region (control volume) as a dashed rectangle/polygon that the user can resize by dragging corners. Multiple pipes pierce the boundary — the user can add up to 5 pipes by clicking on the boundary, each with adjustable diameter and velocity (positive = inflow, negative = outflow). For each pipe, draw an animated flow of colored particles entering or leaving. Inside the control volume, show a fill level that rises (blue fill animating up) when net mass flow is positive and falls when negative. Display a live mass balance scoreboard: Σṁ_in (green) vs Σṁ_out (red) as animated bar charts side by side. When they balance exactly, show a green "STEADY STATE ✓" indicator and the fill level stays constant. When unbalanced, the fill changes and a red "UNSTEADY" indicator flashes. Add a "Steady state challenge" game mode: the system randomly sets 4 of the 5 pipe flows and the student must set the 5th to achieve balance. Show the equation dM_cv/dt = Σṁ_in - Σṁ_out with live substituted values at all times.


Widget D – Difuzor vs. Konfuzor: intuicija ubrzavanja i usporavanja toka
Prompt:

Create an interactive HTML/JS/CSS widget with two side-by-side pipe sections: LEFT = confusor (converging, flow accelerates), RIGHT = diffuser (diverging, flow decelerates). The user controls the area ratio AR = A_outlet/A_inlet with a single slider (0.2 to 5.0). When AR < 1: left pipe is highlighted as active confusor. When AR > 1: right pipe is highlighted as active diffuser. For each case animate: (1) streamlines converging/diverging, (2) velocity arrows growing/shrinking, (3) animated flow particles speeding up or slowing down (particle spacing increases when slow, decreases when fast), (4) a pressure color overlay: red = high pressure (slow flow), blue = low pressure (fast flow) — connecting to Bernoulli preview. Show a live split-panel velocity profile: inlet uniform profile vs outlet uniform profile, with the outlet bar taller (confusor) or shorter (diffuser) than inlet. Display v_outlet = v_inlet / AR updating live. Add a "real-world examples" panel: clicking "Nozzle" sets AR=0.25 with a nozzle illustration, "Diffuser" sets AR=2.25, "Venturi throat" sets AR=0.1. Show the continuity equation A1·v1 = A2·v2 with numbers. Include a velocity ratio gauge showing v2/v1 as a dial.


Widget E – Mješač masenih tokova: konzervacija mase i gustoće
Prompt:

Create an interactive HTML/JS/CSS widget for a T-junction or Y-junction mixer where two fluids of different densities and velocities combine into one outlet stream. Draw the junction with two inlets (top-left and bottom-left) and one outlet (right). Each inlet pipe is colored according to its fluid density (blue = water, yellow = oil, orange = glycerin — selectable from dropdown). Animate colored particle flows in each pipe, with particle density (how many particles per second) proportional to mass flow rate ṁ = ρ·A·v. At the junction, particles from both streams blend into a mixed color in the outlet pipe — the outlet color is a weighted average of the two input colors based on mass fractions. Add sliders for: ṁ1 (1–20 kg/s), ṁ2 (1–20 kg/s), fluid type for each inlet (selects ρ automatically), outlet pipe diameter D_out. Show live: ρ_mix = (ṁ1 + ṁ2)/(V̇1 + V̇2), v_out = (V̇1 + V̇2)/A_out, ṁ_out = ṁ1 + ṁ2. Display a mixing ratio pie chart (% of fluid 1 vs fluid 2 by mass). Add a "density thermometer" visual showing ρ_mix between ρ1 and ρ2. Show all three mass flows as animated bar chart updating live. Include the general mass conservation equation with substituted values.


Widget F – Punjenje i pražnjenje spremnika: dh/dt vizualizacija
Prompt:

Create an interactive HTML/JS/CSS widget simulating a tank being filled or drained through multiple pipes simultaneously. Draw a large rectangular tank with up to 4 pipes (user can enable/disable each): pipes can be set as inlet or outlet with individual velocity and diameter sliders. Show the water level h as an animated blue fill that moves up or down smoothly. On the right side of the tank, show a real-time graph: x-axis = time (scrolling), y-axis = water level h(t) — the curve draws itself in real time as the simulation runs. The slope of the curve visually equals dh/dt. Add a "target level" horizontal dashed line that the user can drag — the challenge is to set pipe flows so that dh/dt = 0 and the level stabilizes exactly at the target. Show a live dh/dt gauge: a speedometer-style dial where center = 0 (steady), right = rising, left = falling. Display the formula dh/dt = (ΣQ_in - ΣQ_out)/A_tank with live substituted values. Add three scenario buttons: "Filling" (Q_in >> Q_out), "Draining" (Q_out >> Q_in), "Steady" (balanced). When level reaches top, animate overflow. When level reaches bottom, animate dry pipe (air bubbles). Include a time-lapse speed slider (1x to 10x simulation speed).


Widget G – Perforirana cijev: distribuirano istrujavanja po duljini
Prompt:

Create an interactive HTML/JS/CSS widget showing a pipe with perforations along its length (like a garden sprinkler or wind tunnel suction wall). Draw a horizontal pipe with N small holes evenly distributed along its length L. Fluid enters at the left with velocity v_in and exits (or is sucked) through each perforation with velocity v_p. Show that the main pipe velocity decreases along the length (for suction case) or increases (for injection case) due to continuity. Animate: (1) main flow particles moving right inside the pipe, getting fewer/slower as fluid leaves through holes, (2) small perpendicular arrows at each hole showing suction (inward) or injection (outward), (3) a velocity profile along the pipe length drawn as a curve above the pipe — it slopes downward for suction. Add sliders for: inlet velocity v_in (10–60 m/s), perforation velocity v_p (1–15 m/s), number of perforations N (10–100), pipe diameter D (0.5–2 m), pipe length L (2–8 m). Display: the continuity equation applied to each incremental section dv/dx = -n_p·(π·d_p²/4)·v_p / A_pipe, v(x) profile updating live. Show Q_inlet, Q_through_holes, Q_outlet all as animated bars summing to zero (for suction). Toggle between suction mode and injection mode — in injection mode, pipe flow accelerates and arrows flip outward.