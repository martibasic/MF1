Zadatak 86 – Sifon s gubicima: raspodjela gubitaka i minimalni tlak
Prompt:

Create an interactive HTML/JS/CSS widget simulating a siphon with friction losses, where the user can control how losses are distributed between sections 1→2 and 2→3. Draw the siphon as an inverted U-tube: point 1 (inlet in left tank at mid-level), point 2 (apex at top), point 3 (exit at bottom right). Add sliders for: pipe diameter d (0.1–0.4 m), h1 (apex height above inlet, 1–4 m), h2 (exit drop below inlet, 0.5–3 m), volumetric flow rate V̇ (0.05–0.3 m³/s), loss fraction f (0–1, where f = fraction of total loss occurring in section 1→2, default 2/3). Compute total loss coefficient K from Bernoulli 1→3, then split: hw1-2 = f·hw_total, hw2-3 = (1-f)·hw_total. Draw the Energy Grade Line (EGL) as a bold red line: starts at total head at point 1, drops by hw1-2 between 1 and 2, drops by hw2-3 between 2 and 3. Draw the Hydraulic Grade Line (HGL) as a blue dashed line below EGL by v²/2g everywhere. Show the pressure at point 2 as a gauge: it is most negative (maximum vacuum) when losses between 1→2 are largest. Animate flow particles through the siphon. Display hw1-2, hw2-3, p2 in Pa updating live. Add a "loss fraction" animation that sweeps f from 0 to 1 showing how p2 changes.


Zadatak 87 – Sapnica i visina mlaza
Prompt:

Create an interactive HTML/JS/CSS widget for a vertical pipe-nozzle system where pressurized water flows up through a wide pipe (D1), through a nozzle (D2), and exits as a vertical jet that rises to height h1 above the nozzle. Draw the system: horizontal inlet at bottom, pipe turning vertical, nozzle at top, vertical jet rising and then falling back down as a parabolic fountain. Add sliders for: D1 (0.1–0.4 m), D2 (0.05–0.2 m, always < D1), gauge pressure at A: pAm (10000–100000 Pa), height of A below nozzle h2 (0.5–3 m), density ρ. Animate: (1) flow particles accelerating through the nozzle, (2) the vertical jet rising to height h1 = pAm/(ρg) + vA²/2g - vjet²/2g - h2, calculated live and shown as the jet height, (3) the jet falling back as a parabola of water droplets. Show the three Bernoulli terms at point A as stacked bars: pressure head pAm/ρg (large, blue), velocity head vA²/2g (small, green), elevation head zA (brown). At the nozzle exit: pressure head = 0 (atmospheric), velocity head = vjet²/2g (large), elevation head = h2. At the jet apex: velocity = 0, all energy is elevation head h1+h2. Display h1 updating live. Show the continuity equation linking vA and vjet via areas.


Zadatak 88 – Cjevovod s tri presjeka, gubici i tlakovi
Prompt:

Create an interactive HTML/JS/CSS widget for a three-section pipeline flowing downhill, with known conditions at section 1 and friction losses between sections. Draw three pipe sections at different elevations (h1, h2, h3 above reference) and different diameters (D1, D2, D3), flowing left to right and downward. Add sliders for: D1 (0.3–0.6 m), D2 (0.1–0.3 m), D3 (0.2–0.4 m), h1 (4–8 m), h2 (2–6 m), h3 (1–4 m), p1 (100000–300000 Pa), v1 (0.5–3 m/s), hw1-2 (0.5–3 m), hw2-3 (1–6 m). From continuity: v2 = v1·A1/A2, v3 = v1·A1/A3. From Bernoulli with losses: p2 = p1 + ρg(h1-h2) + ρ/2(v1²-v2²) - ρg·hw1-2, similarly p3. Draw the EGL starting high at section 1, dropping steeply at each loss section — show the drops as red downward arrows labeled hw1-2 and hw2-3. Draw the HGL (EGL minus v²/2g). Show pressure gauges at all three sections with animated needles. Draw velocity arrows at each section scaled to magnitude — section 2 (narrow) has the longest arrow. Display a results table: v2, v3, p2, p3 all updating live. Color the pipe sections by pressure: red=high, blue=low.


Zadatak 89 – Istjecanje iz velikog spremnika
Prompt:

Create an interactive HTML/JS/CSS widget for flow from a large tank through a hole at the bottom or side, demonstrating v = √(2gh) and the effect of hole position. Draw a tall tank filled with fluid. The user can drag the hole position up and down on the tank wall. Add sliders for: tank height H (1–6 m), hole height above bottom h_hole (0 to H), hole diameter d (10–50 mm), discharge coefficient cv (0.6–1.0). Compute exit velocity v = cv·√(2g·(H - h_hole)) and display it as an arrow at the hole. Animate a horizontal jet leaving the hole as a parabolic stream of particles landing on the ground. Show the Bernoulli derivation panel: at surface p=patm, v≈0, z=H; at hole p=patm, v=v2, z=h_hole → v2=√(2g·(H-h_hole)). Add a "multiple holes" mode: show 5 holes at equal spacing h/H = 0.1, 0.3, 0.5, 0.7, 0.9 simultaneously, each shooting a different-colored jet — visually confirm that the hole at h/H=0.5 (mid-height) gives the longest range. Show landing distances for all holes. Display v2 and landing distance x = 2·√(h_hole·(H-h_hole)) live.


Zadatak 90 – Pitotova cijev: stagnacijska točka i mjerenje protoka
Prompt:

Create an interactive HTML/JS/CSS widget simulating a Pitot-static tube inserted into a pipe flow, connected to a differential mercury manometer. Draw a pipe cross-section (diameter d) with fluid flowing right. The Pitot tube opening faces the flow at the pipe centerline — animate streamlines bending around the tube tip and show the stagnation point where v=0. The static tap is on the pipe wall where velocity is v (undisturbed). Connect both taps to a U-tube manometer filled with mercury (dark grey, dense). Animate the mercury level difference ΔhHg responding to the flow velocity: faster flow = larger ΔhHg. Add sliders for: pipe diameter d (0.1–0.4 m), flow velocity v (0.5–8 m/s), fluid density ρ (800–1200 kg/m³), mercury density ρHg (13600 fixed). Show the Bernoulli derivation: at stagnation point p_stag = p_static + ρv²/2, so Δp = ρv²/2, and ΔhHg = Δp/(g·(ρHg-ρ)). Display ΔhHg in mm updating live. Show V̇ = A·v. Add a "reverse problem" mode: user sets ΔhHg with a slider and must calculate v — show the formula v = √(2g·ΔhHg·(ρHg-ρ)/ρ) with live values.


Zadatak 91 – Stratificirani višefazni spremnik: istjecanje ulja
Prompt:

Create an interactive HTML/JS/CSS widget for a large pressurized tank with four distinct layers from bottom to top: water (blue), gasoline/benzin (yellow), oil (orange), and pressurized air (grey) at the top. An exit port at the oil layer level allows oil to flow out. Add sliders for: air gauge pressure pm (50000–200000 Pa), water layer height h1 (1–4 m), oil layer height h2 (2–6 m), gasoline layer height h3 (0.5–3 m), exit diameter d (10–40 mm), gasoline relative density ρ̄b (0.6–0.75), oil relative density ρ̄u (0.75–0.90). Draw the tank with clearly colored horizontal layers, labels, and height dimension lines. Animate oil flowing out of the exit port as a stream of orange particles. Compute pressure at the oil surface (point 1): p1 = pm + ρb·g·h3, then apply Bernoulli to find exit velocity v2 using only oil density: v2 = √(2·p1/ρu - 2g·h2). Show why only oil density appears in the exit velocity formula — highlight this in a physics explanation panel. Display ṁ = ρu·A·v2 updating live. Show how pm affects v2 dramatically (pressurized tank = much faster exit). Animate the layer levels slowly dropping as oil flows out.


Zadatak 92 – Protočna sapnica u cijevi: koeficijent kontrakcije
Prompt:

Create an interactive HTML/JS/CSS widget for a flow nozzle (orifice plate/nozzle) inserted in a pipe, connected to a differential mercury manometer below. Draw the pipe with the nozzle constriction inside — show the vena contracta (actual minimum flow area) as slightly smaller than the geometric nozzle area, represented by the contraction coefficient cC. Add sliders for: pipe diameter dc (0.1–0.4 m), nozzle diameter ds (0.02–0.1 m), mercury manometer height h (0.005–0.1 m Hg), contraction coefficient cC (0.6–1.0). Animate: (1) flow streamlines converging smoothly into the nozzle and then diverging after, (2) the vena contracta region highlighted in red just downstream of the nozzle where actual flow area = cC·AB, (3) mercury manometer with animated fluid column difference ΔhHg. Compute Δp = g·h·(ρHg - ρv) and display it. Show the derivation of the flow formula step by step: Bernoulli A→B + continuity → vA formula → multiply by cC·AA → V̇. Display V̇ updating live. Show a comparison between ideal (cC=1) and real (cC<1) flow rates as two bars side by side. Explain physically: vena contracta means effective area is smaller than geometric area.


Zadatak 93 – Slobodna površina kanala: izbočenje i ulegnuće
Prompt:

Create an interactive HTML/JS/CSS widget for open channel flow over a bottom hump (bump), showing how the free surface responds with a dip directly above the bump — linking velocity increase to surface depression. Draw a horizontal rectangular channel (side view) with fluid flowing left to right at depth h. A smooth bump of height δ rises from the channel floor in the middle. The free surface shows a depression of depth d directly above the bump. Add sliders for: channel depth h (0.3–2 m), bump height δ (0.05–0.5 m, always < h), free surface dip d (0.01–0.3 m), channel width (unit width). Animate: (1) streamlines that are closer together above the bump (higher velocity), (2) the free surface curve dipping by d above the bump and recovering after, (3) flow particles speeding up over the bump and slowing after. Show the Bernoulli equation applied at the free surface: v1²/2g + h = v2²/2g + (h-d), combined with continuity h·v1 = (h-d-δ)·v2. Display V̇ per unit width = h·v1 updating live. Add a "why does the surface dip?" explanation: as flow accelerates over the bump (continuity), velocity head increases, so elevation head must decrease (Bernoulli) → surface drops. Show the energy equation graphically.


Zadatak 94 – Venturi ejektor: podizanje tekućine
Prompt:

Create an interactive HTML/JS/CSS widget for a Venturi-type ejector/aspirator that uses the low pressure at the throat to suck liquid up from a reservoir below. Draw the Venturi pipe (wide inlet d1, narrow throat d2, wide outlet) oriented horizontally. Below the throat, draw a vertical suction tube descending to a liquid reservoir — the liquid is sucked upward by the low pressure at the throat. Add sliders for: inlet velocity v1 (1–15 m/s), d1 (0.1–0.3 m), d2 (0.03–0.15 m), suction height h (0.1–3 m), fluid density ρ. Compute throat pressure from Bernoulli: p_throat = p1 + ρ/2·(v1²- v2²) where v2 = v1·(d1/d2)². Show the suction tube with animated liquid rising — the liquid level rises higher as throat velocity increases (lower pressure). Show the minimum v2 required to lift liquid to height h: p_throat must be < patm - ρgh. Display a "suction gauge" showing how much below atmospheric the throat pressure is. Animate: when v2 is too low, liquid doesn't rise; when v2 exceeds critical value, liquid rises and flows through. Show the formula for minimum v2 = √((2/ρ)·(ρgh + (p1-patm)) / (1-(d2/d1)⁴)) updating live.


Zadatak 95 – Venturi cijev s manometrom: protok s gubicima i bez
Prompt:

Create an interactive HTML/JS/CSS widget for a Venturi flowmeter connected to an air-water differential manometer, with a side-by-side comparison of ideal (no losses) vs real (with losses) flow rates. Draw the Venturi pipe (inlet d1, throat d2) oriented at a slight angle (not necessarily horizontal), with a vertical U-tube manometer connecting inlet and throat — the manometer tube is filled with air at the top and water below, showing height difference h. Add sliders for: d1 (0.2–0.5 m), d2 (0.1–0.25 m), manometer height h (0.05–0.5 m), loss coefficient K_loss (0–0.5, applied as K·v1²/2g), density ρ (800–1200 kg/m³). Animate: (1) flow particles accelerating through throat, (2) manometer levels animating with h difference, (3) streamlines converging. Compute Δp from manometer: p1 - p2 = ρ·g·h (air density neglected). Show TWO calculated flow rates side by side as animated bars: V̇_ideal (blue, no losses) and V̇_real (red, with losses) — the real flow rate is always smaller. Display the percentage difference between ideal and real as losses increase. Show the formula derivation panel. Add an "efficiency" indicator: V̇_real/V̇_ideal × 100%. Update everything live.


Widget A – Vizualizacija gubitaka: idealni vs. realni fluid
Prompt:

Create an interactive HTML/JS/CSS widget that directly compares ideal fluid (no losses) vs real fluid (with friction losses) flowing through the same pipe system side by side. Draw two identical pipe systems next to each other: LEFT = ideal fluid, RIGHT = real fluid. Both pipes have the same geometry: horizontal section, uphill section, narrow section, downhill section. Add sliders for: inlet velocity v0 (1–10 m/s), pipe length L (5–30 m), friction factor f (0.01–0.08, Darcy-Weisbach), pipe diameter D (0.05–0.3 m), fluid density ρ. For the IDEAL case: draw the EGL as a perfectly horizontal red line (total head constant everywhere), HGL undulates following pipe geometry. For the REAL case: draw the EGL as a continuously SLOPING downward red line — the slope equals f·v²/(2g·D) per unit length (head loss per meter). The gap between the two EGL lines grows along the pipe length — this gap IS the accumulated head loss. Animate flow particles: in both pipes particles move at the same inlet speed, but in the real pipe they slow down slightly (pressure drops). Show a live "energy loss meter" — a red bar growing from left to right as flow progresses downstream in the real pipe. Display total head loss hw = f·L·v²/(2g·D) (Darcy-Weisbach formula) updating live. Add a "zoom to losses" button that magnifies the EGL difference.


Widget B – Darcy-Weisbach: gubici u ravnoj cijevi
Prompt:

Create an interactive HTML/JS/CSS widget dedicated to the Darcy-Weisbach head loss equation hw = f·L·v²/(2g·D), allowing students to explore how each parameter affects pipe friction losses. Draw a straight horizontal pipe with flow moving left to right. Show the EGL sloping downward — the slope visually represents the hydraulic gradient S = hw/L. Add individual sliders for: pipe length L (1–100 m), pipe diameter D (0.02–0.5 m), flow velocity v (0.1–10 m/s), Darcy friction factor f (0.008–0.08). Show four separate bar charts, one for each parameter's contribution: doubling L doubles hw (linear), halving D increases hw by factor 2⁵ (strong effect), doubling v quadruples hw (quadratic — highlight this!), doubling f doubles hw (linear). Animate the pipe cross-section: show the friction as small resistance arrows on the pipe walls pointing against flow direction, with arrow density proportional to f. Show the velocity profile inside the pipe (parabolic for laminar, flatter for turbulent). Display a sensitivity panel: "Which parameter has the STRONGEST effect on hw?" — rank them automatically from most to least sensitive based on current values. Show hw in meters and equivalent pressure drop Δp = ρ·g·hw in Pa. Add a "Reynolds number" display and indicate laminar/turbulent regime.


Widget C – Lokalni gubici: koljena, ventili, proširenja
Prompt:

Create an interactive HTML/JS/CSS widget for local (minor) head losses through pipe fittings, expressed as hw = K·v²/2g where K is the loss coefficient. Draw a pipe system that the user assembles from a component palette on the left: drag and drop fittings into the pipe — available components are: sharp entrance (K=0.5), smooth entrance (K=0.04), 90° sharp elbow (K=0.9), 90° smooth bend (K=0.2), sudden expansion (K computed from areas), sudden contraction (K=0.5), gate valve (K=0.1–10 depending on opening %), globe valve (K=10), exit into tank (K=1.0). As each component is added, draw it in the pipe and instantly show its local EGL drop as a step downward — a sudden vertical drop on the EGL, proportional to K·v²/2g. Show the total EGL as the sum of all component drops plus any pipe friction. Display a running loss budget: a bar chart with each component's contribution color-coded. Add an "opening percentage" slider for the gate valve — as it closes from 100% to 0%, K increases dramatically from 0.1 to infinity and the EGL drop for that valve grows, eventually stopping flow entirely. Show the formula hw_total = Σ(K_i · v²/2g) + f·L·v²/(2g·D) with all terms listed. Animate flow particles slowing after each loss element.


Widget D – Hidraulički gradijent i piezometarska linija
Prompt:

Create an interactive HTML/JS/CSS widget that teaches the difference between the Energy Grade Line (EGL), Hydraulic Grade Line (HGL), and piezometric head through an interactive pipe system. Draw a pipe with varying diameter and elevation, flowing from a pressurized tank (left) to an open exit (right). The user can click anywhere on the pipe to place a "piezometer tube" — a vertical glass tube rising from that point. Animate the water level in each piezometer rising to the HGL height at that point: h_piezo = p/ρg + z. If the HGL is below the pipe centerline at that point, the piezometer shows vacuum (water is sucked up into the tube rather than rising). Add sliders for: tank pressure, pipe geometry (diameters and elevations at 5 control points), flow rate, friction factor. Draw three lines: EGL (bold red, sloping down), HGL (blue, always below EGL by v²/2g), pipe centerline (grey). Color-code pipe sections: GREEN where HGL is above pipe (positive gauge pressure), RED where HGL is below pipe (negative gauge pressure = suction — cavitation risk). When the user places a piezometer in a red section, animate it showing bubbles (cavitation). Display at every clicked point: p/ρg, v²/2g, z, and total H. Add a "where is cavitation risk?" auto-highlight button.


Widget E – Pumpa u sustavu: radna točka i visina dobave
Prompt:

Create an interactive HTML/JS/CSS widget for a pump system, showing how the pump operating point is determined by the intersection of the pump characteristic curve and the system curve. Draw a pump system: suction tank (left, lower) → suction pipe → pump → discharge pipe → delivery tank (right, higher). The SYSTEM CURVE shows required head H_sys = Δz + (f·L/D + ΣK)·v²/2g as a parabola opening upward — minimum head needed at zero flow = static head Δz, increasing with flow due to friction. The PUMP CURVE shows available head H_pump as a downward curve (high head at zero flow, decreasing with flow rate) — user can select pump type from dropdown (centrifugal slow/fast, axial). The OPERATING POINT is where the two curves intersect — shown as a bold green dot that moves when parameters change. Add sliders for: static head Δz (2–20 m), pipe length L (10–200 m), diameter D (0.05–0.3 m), friction factor f, pump speed N (50–150% of nominal — scaling the pump curve up/down). Animate: as D increases, system curve flattens → operating point moves right (more flow). As Δz increases, system curve shifts up → less flow at operating point. Show the EGL diagram below: suction side (EGL dropping), pump (EGL jumping up by H_pump), discharge side (EGL dropping to delivery tank level). Display operating flow rate V̇ and pump head H in real time.


Widget F – Bernoulli s gubicima: interaktivno rješavanje
Prompt:

Create an interactive HTML/JS/CSS widget that is a step-by-step visual solver for Bernoulli's equation with losses: H1 = H2 + hw. Draw a generic pipe system where the user defines two cross-sections (1 and 2) by setting: pressure p (gauge, via slider), velocity v (via slider or computed from flow rate and diameter), elevation z (draggable on canvas), and head loss hw between the sections (via slider). The widget shows the Bernoulli equation with all six terms (three at each section plus hw) as an animated balance scale: LEFT pan = H1 = p1/ρg + v1²/2g + z1, RIGHT pan = H2 + hw = p2/ρg + v2²/2g + z2 + hw. When the equation is balanced, the scale is level and shown in green. When unbalanced, the scale tilts and shows in red — the student must adjust the unknown quantity until balance is achieved. Add a "solve for" dropdown: solve for p2, or v2, or z2, or hw — the selected unknown becomes a slider the student must tune. Show each term as a colored block stacking on the balance pan: blue=pressure head, green=velocity head, brown=elevation head, red=losses. Animate the blocks resizing as sliders change. Include three preset scenarios: "pump system", "siphon with losses", "nozzle flow" — each auto-configures the geometry and asks the student to find the unknown.


Widget G – Reynoldsov broj: laminarno vs. turbulentno strujanje
Prompt:

Create an interactive HTML/JS/CSS widget that visualizes the transition from laminar to turbulent pipe flow as Reynolds number Re = ρ·v·D/μ changes, and connects this to friction losses. Draw a pipe cross-section (side view) showing the flow pattern inside. Add sliders for: flow velocity v (0.001–5 m/s), pipe diameter D (0.01–0.3 m), fluid viscosity μ (0.001 for water to 1.0 for oil, Pa·s), fluid density ρ (800–1200 kg/m³). When Re < 2300: animate smooth parallel streamlines (laminar) — all moving in straight lines, faster at center, zero at walls. Show the parabolic velocity profile. When 2300 < Re < 4000: show wavy, unstable streamlines (transition) — animate them oscillating and occasionally breaking up. When Re > 4000: animate chaotic, mixing eddies (turbulent) — particles move in random zigzag paths while overall flow goes left to right. Show a large Re number display that changes color: green (laminar), yellow (transition), red (turbulent). Show the friction factor f: for laminar f = 64/Re (decreasing line), for turbulent f from Moody chart (roughly constant or slowly decreasing). Display hw = f·L·v²/(2g·D) for a fixed L=10m pipe, showing how it changes with regime. Add a "Moody diagram" panel showing the current operating point as a moving dot.


Widget H – Višestruki cjevovodi: serijski i paralelni spoj
Prompt:

Create an interactive HTML/JS/CSS widget demonstrating head loss behavior in series and parallel pipe systems, with a toggle between the two configurations. Draw two layouts: SERIES mode (pipes connected end to end: pipe 1 → pipe 2 → pipe 3) and PARALLEL mode (three pipes connecting the same two nodes A and B, side by side). Add sliders for each pipe: length L (5–50 m), diameter D (0.05–0.3 m), friction factor f (0.01–0.08), plus total flow rate V̇_total. SERIES mode: show that total head loss = sum of individual losses, but flow rate is the same in all pipes. Draw the EGL stepping down with each pipe section — steeper sections (smaller D, larger L, larger f) drop faster. PARALLEL mode: show that head loss is EQUAL across all parallel branches, but flow distributes proportionally to each pipe's conductance. Animate: wider/smoother pipes carry more flow (more particles), narrow/rough pipes carry less. Show flow split percentages for each parallel branch updating live. Display a comparison table: hw_total(series) vs hw_total(parallel) for the same total flow — parallel is always lower (less resistance). Add a "design challenge": given a target flow rate and maximum allowed head loss, which configuration achieves it? Show the equivalent single pipe diameter D_eq for both cases.
