Zadatak 49 – Cilindrična automatska zaklopka
Prompt:

Create an interactive HTML/JS/CSS widget simulating a solid cylinder used as an automatic gate flap, hinged at point A, resting against the tank floor. The cylinder is submerged with water on the left side. Add sliders for: water level h1 (1–7 m), cylinder radius R (0.3–1.2 m), h2 (depth from surface to hinge A), and fluid density ρ. Draw the tank cross-section with the cylinder visible as a circle, hinge A marked, and water on the left. Decompose the hydrostatic force into: horizontal component F_H (force on vertical projection = rectangle R×b) shown as a leftward arrow, and vertical component F_V (weight of fluid volume above the curved surface) shown as a downward arrow. Draw the resultant F_R as a diagonal arrow from the center of pressure. Show the cylinder weight G_c as a downward arrow from the cylinder center. Animate: when h1 rises above the critical opening level, show the cylinder rotating open around A with a smooth animation. Display the angle α of F_R from horizontal. Show the moment balance about A: when M_water > M_weight, the gate opens. Update all force arrows and values live with sliders.


Zadatak 50 – Zakrivljena površina, četvrtina kruga, voda s lijeva
Prompt:

Create an interactive HTML/JS/CSS widget for hydrostatic forces on a quarter-circle curved surface AB (concave, water above and to the left), with point A at the top right and B at the bottom. Add sliders for: radius R (0.5–2.5 m), water depth above A: h1 (0.5–4 m), tank width b (0.5–3 m), and fluid density ρ. Draw the quarter-circle surface with water filling the region above and to the left. Show three labeled regions with different colors: the rectangular fluid block AFCD (height h1, width R) and the quarter-circle fluid block ABF. Animate two separate force arrows: F_H (horizontal, pointing left, acting on vertical projection R×b at depth z_FH) and F_V (vertical, pointing downward, equal to weight of fluid volume above the curve). Show the centroid of the vertical projection and the point C_FH marked. Display an animated dashed line for the line of action of F_H at depth z_FH. Show a separate panel with the volume decomposition: V_total = V_rectangle + V_quarter_circle, each colored differently. Update F_H, F_V, z_FH, x_FV numerically in real time. Add a button to toggle between showing/hiding the imaginary fluid volume above the surface.


Zadatak 51 – Zakrivljena površina, četvrtina kruga, voda s desna
Prompt:

Create an interactive HTML/JS/CSS widget for hydrostatic forces on a quarter-circle curved surface AB where water is on the RIGHT side (convex surface from water's perspective), point A at the top left, B at bottom right. Add sliders for R, h1, b, ρ. Draw the quarter-circle with water to the right and below. Show that F_H still equals the force on the vertical projection (same magnitude as Task 50 but reversed direction — pointing RIGHT). Show F_V pointing UPWARD (buoyancy from imaginary fluid volume below the surface). Animate both components with arrows. Highlight the key physics difference from Task 50: the vertical component is now upward because the imaginary fluid volume is below the curved surface, not above. Show a side-by-side comparison panel: left = Task 50 geometry (water left, F_V down), right = Task 51 geometry (water right, F_V up). Animate the volume ABCD below the curve filled with a ghostly blue to represent the imaginary fluid. Show x_FV position of the vertical force. Display moment balance equations. Update all values live.


Zadatak 52 – Zakrivljena površina, šestina kružnog isječka
Prompt:

Create an interactive HTML/JS/CSS widget for hydrostatic forces on a curved gate AB that is a 1/6th arc of a circle (60° sector), with the center O at the bottom right, A at the top of the arc, and B at the bottom left. Water fills the region above and to the left of the arc. Add sliders for: radius R (2–10 m), arc angle α (30°–90°), tank length b (5–20 m), and ρ. Draw the arc from A to B with center O, showing the coordinate system. Shade three sub-volumes used in the volume calculation: rectangle ACBD (R·sinα × (R - R·cosα)), triangle DBO, and the sector ABO — each in a different color with a label. Animate F_H as a leftward arrow on the vertical projection BD (height R·sinα). Animate F_V as a downward arrow at position x_FV from AC. Show the centroid T of the curved surface area ABC. Display: z_FH = R·sinα/3 from bottom, x_FV from the moment balance. Show the sector centroid formula OT = 2R/π·cos(α/2) with a labeled diagram. Update all areas, volumes, forces, and positions live when sliders change. Add a toggle to show/hide each sub-volume contribution.


Zadatak 53 – Elastični balon, uzgon i promjena tlaka
Prompt:

Create an interactive HTML/JS/CSS widget showing an elastic balloon tethered by a rope to the bottom of a partially water-filled tank, with pressurized air above the water surface. Add sliders for: surface pressure p1 (100–1600 kPa), initial balloon diameter D1 (0.1–0.5 m), water depth h1 (0.2–1 m), and density ρ. Draw the tank cross-section with: water (blue), air space above (light grey), the balloon as a circle whose diameter shrinks in real time as p1 increases, and a vertical rope from balloon bottom to tank floor. Animate the balloon diameter changing according to D = D1·√(p_initial/p_current) as you move the pressure slider — the balloon visibly shrinks. Show two force arrows on the balloon: F_LB (buoyancy, upward, proportional to D³) and G_B (weight, zero per assumption). Show the rope tension F_U = F_LB as a downward arrow at the rope. Display a live graph panel (pressure on x-axis, buoyancy force on y-axis) showing the curve F_LB ∝ 1/p^(3/2). Show the percentage change ΔF_U updating live. Add a "Play pressure increase" animation button that gradually increases p from p1 to p2 showing the balloon shrinking in real time.


Zadatak 54 – Ledenjak, uzgon i prevrtanje
Prompt:

Create an interactive HTML/JS/CSS widget simulating an iceberg floating in seawater. Add sliders for: iceberg density ρ_ice (850–950 kg/m³), seawater density ρ_sea (1000–1100 kg/m³), and an asymmetry parameter for melting (0–100%). Draw a cross-section of the ocean with the iceberg: the submerged portion shown in dark blue, the above-water portion in white/light blue. The submerged fraction V_um/V_L = ρ_ice/ρ_sea updates live as sliders change — the visible above-water portion of the iceberg grows/shrinks accordingly. Show two force arrows: G (gravity, downward from center of mass) and F_L (buoyancy, upward from centroid of submerged volume). When the melting asymmetry slider increases, animate the iceberg shape becoming irregular (one side melts more), causing the center of mass to shift horizontally. Show that when G and F_L are no longer vertically aligned, a net torque is created — animate the iceberg slowly rotating/tipping. Add an info panel showing: submerged % = ρ_ice/ρ_sea × 100, and a stability indicator (stable / tipping). Display the physics equation G = F_L → ρ_L·V_L = ρ_mv·V_um with live values.


Zadatak 55 – Obrnuti cilindrični spremnik s manometrom
Prompt:

Create an interactive HTML/JS/CSS widget simulating an upside-down cylindrical container partially submerged in water, trapping air inside, connected to a U-tube manometer. Add sliders for: container weight G (20–150 N), diameter D (0.1–0.5 m), submersion depth h2 (0.05–0.4 m), manometer fluid relative density ρ_mf (1.5–3.0), and water density ρ_v. Draw the cross-section: the inverted container with trapped air inside (grey), water outside (blue), the manometer U-tube on the right side with denser manometer fluid (orange). Animate the manometer fluid column height h1 responding to changes in trapped air pressure — as the container is pushed deeper (h2 increases), air pressure increases and h1 increases accordingly. Show pressure balance equation at points A and B (bottom of container): p_air = ρ_v·g·h2 with live values. Show the force balance: upward buoyancy on the container walls vs downward weight G and applied force F. Animate a downward force arrow F from the top. Display h1 = (ρ_v/ρ_mf)·h2 updating live. Add a "Push deeper" animation that smoothly increases h2, showing h1 rising simultaneously. Show all force arrows (G, F, buoyancy) with live magnitudes.