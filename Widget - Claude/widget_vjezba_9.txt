Zadatak 76 – Torricellijev zakon i slobodni mlaz
Prompt:

Create an interactive HTML/JS/CSS widget simulating fluid ejecting as a free jet from a hole in a tank wall at height h above the ground, with total fluid height H above the ground. Add sliders for: total tank height H (1–5 m), hole height h (0.1·H to 0.9·H, always below H), and a velocity coefficient cv (0.6–1.0, default 1.0 for ideal fluid). Draw the tank as a rectangle with blue water fill up to H. The hole is at height h on the right wall. Animate the parabolic jet trajectory: x(t) = v0·t, y(t) = h - g·t²/2, where v0 = cv·√(2g·(H-h)). Draw the animated parabolic arc as a blue curve from the hole to where it hits the ground (y=0). Show animated water particles traveling along the parabola. Display the landing distance x = 2·√(h·(H-h)) updating live. Add three marker dots on the tank wall at h/H = 0.25, 0.50, 0.75, each launching a separate jet arc in a different color — show all three simultaneously and mark their landing x positions. Highlight that h=H/2 gives maximum range x=H. Show the Torricelli formula v0 = √(2g·(H-h)) and Bernoulli derivation panel. Add a "drain animation" button that slowly lowers H showing the jet arc changing shape in real time.


Zadatak 77 – Laminaran profil brzine u cijevi: paraboloid
Prompt:

Create an interactive HTML/JS/CSS widget visualizing the laminar (parabolic) velocity profile inside a circular pipe cross-section. Draw the pipe in two views side by side: LEFT = side view (2D cross-section showing the parabolic profile as a curve), RIGHT = front view (circular cross-section with color-coded velocity: dark blue at center = fast, white at walls = zero). Add sliders for: pipe radius r0 (0.05–0.3 m), maximum velocity vmax (0.5–5 m/s). In the side view, draw the velocity profile curve v(r) = vmax·[1-(r/r0)²] as a filled parabola. Animate flow particles as horizontal dots moving right: center dots move fastest, wall dots move slowest. Show an animated thin ring at radius r with width dr (draggable by user) — display the local velocity v(r) and the flow contribution dQ = v(r)·2π·r·dr of that ring. Show the integration visually: as r sweeps from 0 to r0, color-fill the area under the v(r)·r curve and show V̇ accumulating. Display the proof: v_sr = V̇/A = vmax/2 with live numerical verification. Show the paraboloid shape in the right panel as a 3D-like shaded cone. Add a toggle to compare with uniform (turbulent) profile at same flow rate.


Zadatak 78 – Bernoullijeva jednadžba: tlačna visina dobave
Prompt:

Create an interactive HTML/JS/CSS widget for flow through a pipe of varying cross-section at two different elevations, demonstrating the total head H = p/ρg + v²/2g + z = constant. Draw a pipe that goes upward from point A (lower, smaller diameter dA) to point B (higher, larger diameter dB). Show a reference horizontal line at the bottom. Add sliders for: dA (0.1–0.5 m), dB (0.2–0.8 m), volumetric flow rate V̇ (0.1–1.0 m³/s), elevation zA (1–5 m), elevation zB (3–10 m). Compute and display three colored bars at both points A and B stacked vertically: (1) pressure head p/ρg (blue bar), (2) velocity head v²/2g (green bar), (3) elevation head z (brown bar). The total height of all three bars = H_total must be the same at A and B (conservation). As sliders change, watch the bars redistribute: when pipe widens at B, velocity drops so green bar shrinks, but since total H is conserved, blue bar grows (pressure recovers). Animate flow particles moving faster at A (narrow) and slower at B (wide). Draw the hydraulic grade line (HGL = p/ρg + z) and energy grade line (EGL = H_total) as horizontal dashed lines. Display H_A and H_B numerically, confirming they are equal.


Zadatak 79 – Bernoulli u ventilacijskom kanalu: promjena tlaka
Prompt:

Create an interactive HTML/JS/CSS widget for horizontal airflow through a converging duct (cross-section area A1 reduces to A2). Add sliders for: inlet area A1 (0.02–0.15 m²), outlet area A2 (0.01–0.05 m², always < A1), mass flow rate ṁ (0.2–2.0 kg/s), air density ρ (1.0–1.4 kg/m³). Draw the duct as a horizontal converging shape. Animate streamlines that pack together as the duct narrows. Show velocity arrows at cross-sections 1 and 2, scaling with v = ṁ/(ρ·A). Display two animated pressure gauges (round dial style) at sections 1 and 2: gauge 1 shows higher pressure (wider section, slow flow), gauge 2 shows lower pressure (narrow section, fast flow) — needles move in real time. Show the Bernoulli equation Δp = ρ/2·(v2²-v1²) with live substituted values. Draw a pressure-position graph below the duct: x-axis = position along duct, y-axis = pressure — show a smooth drop from p1 to p2 as flow accelerates. Add a color overlay on the duct: red (high pressure, slow) transitioning to blue (low pressure, fast). Display Δp in Pa updating live. Add a "reverse flow" toggle that swaps inlet/outlet to show pressure recovery in a diffuser.


Zadatak 80 – Pumpa: visina dobave i podtlak na usisu
Prompt:

Create an interactive HTML/JS/CSS widget for a pump system drawing oil from a tank, showing the concept of total head H = p/ρg + v²/2g + z at the suction point A. Draw the system: oil tank on the left (lower), suction pipe going up to pump, discharge pipe going up to upper tank on the right. Label point A on the suction pipe at height hA above tank bottom. Add sliders for: pipe diameter d1 (50–200 mm), volumetric flow rate V̇ (0.01–0.1 m³/s), oil relative density ρ̄ (0.7–0.95), manometer reading ΔhHg (−0.05 to −0.40 mHg, negative = vacuum), height hA (0.5–3 m). Show an animated U-tube mercury manometer at point A with the mercury column visually displaced by ΔhHg — the column is lower on the pipe side (vacuum). Animate oil flowing from lower tank through suction pipe to pump. Display the three head components at A as stacked bars: pressure head p_A/ρ·g (negative, red bar going down below zero), velocity head v²/2g (green), elevation head z_A (brown). Show the total head H_A = sum of all three, updating live. Explain: negative pressure head = vacuum/suction. Show conversion: p_A = ρ_Hg·g·ΔhHg in Pa.


Zadatak 81 – Pumpa s usisnom i tlačnom granom: visine dobave A i B
Prompt:

Create an interactive HTML/JS/CSS widget for a complete pump system with suction branch (point A, diameter d1) and discharge branch (point B, diameter d2, open to atmosphere at top). Draw the full system cross-section: lower tank → suction pipe with point A → pump → rising discharge pipe → point B at top (open end). Show a reference line at the tank bottom. Add sliders for: d1 (0.15–0.35 m), d2 (0.1–0.3 m), h1 (suction point height, 3–12 m), h2 (pump height, 8–18 m), h3 (discharge end height, 15–30 m), V̇ (0.05–0.2 m³/s), vacuum at A in mmHg (−0.1 to −0.5). Animate water flowing through the entire system with colored moving particles. Show the energy grade line (EGL) as a continuous colored line: it drops along pipes (friction — set to zero here), then JUMPS UP across the pump (pump adds energy), then continues to B. Show the hydraulic grade line (HGL) below the EGL by v²/2g everywhere. Display H_A and H_B as stacked bar charts updating live at their respective positions. Show the pump head H_pump = H_B - H_A + head losses. Label all heights h1, h2, h3 with dimension lines.


Zadatak 82 – Bernoulli s gubitcima: pad visine dobave
Prompt:

Create an interactive HTML/JS/CSS widget for flow through a pipe system where total head H decreases from point 1 to point 2 due to friction losses. Draw a pipe that goes horizontally then drops to a lower elevation: point 1 (upper left, smaller diameter d1) to point 2 (lower right, larger diameter d2). Add sliders for: d1 (0.1–0.3 m), d2 (0.15–0.35 m), volumetric flow rate V̇ (0.02–0.15 m³/s), inlet pressure p1 (200000–600000 Pa), height difference h1-h2 (0.5–4 m), oil relative density ρ̄ (0.7–0.95). Draw the energy grade line (EGL) starting high at point 1, then show it SLOPING DOWNWARD due to head loss h_loss = H1 - H2 — this slope represents energy dissipation. Show the hydraulic grade line (HGL) below EGL by v²/2g. Display stacked head bars at points 1 and 2: notice that the total at 2 is LOWER than at 1 by exactly h_loss. Show Bernoulli WITH losses: H1 = H2 + h_loss. Calculate and display p2 from the modified Bernoulli equation. Use a red zone to highlight the head loss between 1 and 2. Animate flow particles. Show all units and formulas with live numerical substitution.


Zadatak 83 – Sifon s gubicima: protok i podtlak u vrhu
Prompt:

Create an interactive HTML/JS/CSS widget simulating a siphon tube (inverted U-shape) drawing oil from one tank and discharging at a lower level, with friction losses. Draw the siphon: tank on the right at mid-level, tube goes UP to point 2 (apex, highest point, height h2 above tank surface), then DOWN to exit point 3 (lowest point, height h2+h1 below apex total drop). Add sliders for: tube diameter d (20–80 mm), h1 (height of apex above inlet, 1–4 m), h2 (total drop from apex to exit, 3–8 m), loss coefficients hw1-2 (0.5–3.0 m), hw2-3 (0.5–4.0 m), oil relative density ρ̄ (0.75–0.95). Animate oil flowing: entering at point 1, rising upward (note: this is counterintuitive — animate particles moving against gravity up to the apex), then accelerating downward to exit at point 3. Show pressure along the siphon as a color overlay: green near atmospheric at entry, darkening to red (high vacuum) at the apex point 2, returning to green at exit. Display a pressure gauge at point 2 showing the sub-atmospheric (vacuum) pressure with a needle in the negative zone. Show the Bernoulli equation applied from 1→3 (with losses) to find v3, then 1→2 to find p2. Display V̇ = A·v3 live. Add a "cavitation warning" indicator that flashes when p2 approaches vapor pressure.


Zadatak 84 – Sifon: gubici proporcionalni v², apsolutni tlak
Prompt:

Create an interactive HTML/JS/CSS widget for a siphon where head losses are expressed as multiples of the velocity head v²/2g: loss 1→2 = 2v²/2g, loss 2→3 = 3v²/2g (total loss = 5v²/2g). Draw the siphon geometry with point 1 (inlet in tank), point 2 (apex, height h2 above tank level), point 3 (exit, height h2-h1 below tank level if discharge is lower). Add sliders for: d (40–100 mm), h1 (1–3 m), h2 (3–6 m), oil relative density ρ̄ (0.7–0.95), atmospheric pressure patm (90000–105000 Pa). Show the Bernoulli equation with loss terms: p1/ρg + 0 + z1 = p3/ρg + v²/2g + z3 + 5v²/2g → solve for v. Display v updating live as sliders change. Show the resulting V̇ = A·v. For pressure at point 2, show the Bernoulli equation 1→2 with loss = 2v²/2g: compute gauge pressure p2m and absolute pressure p2abs = p2m + patm. Draw an absolute pressure scale at point 2: show patm as a reference line, the actual p2abs below it (vacuum), and mark the vapor pressure limit. Animate flow particles whose speed reflects v. Show a loss budget bar chart: useful head available (h2-h1) split between kinetic energy at exit (1/6 of total) and losses (5/6 of total).


Zadatak 85 – Sifon: izlazna brzina i minimalni tlak u vrhu
Prompt:

Create an interactive HTML/JS/CSS widget proving two key results for an ideal siphon (no losses): (1) exit velocity v2 depends only on h3 (height difference between inlet surface and outlet), and (2) minimum pressure occurs at the apex (point 3) and depends on h2+h3. Draw the siphon with three labeled points: point 1 (free surface in right tank, height h1 above outlet), point 3 (apex, height h2 above tank surface), point 2 (exit at bottom, open to atmosphere). Add sliders for h1 (0.2–1.5 m), h2 (1–5 m), h3 (2–6 m), fluid density ρ (800–1100 kg/m³), patm (90000–105000 Pa). Animate fluid flowing through the siphon with particles. Show live: v2 = √(2g·h3) — note it does NOT depend on h2 or h1 — demonstrate this by changing h2 and showing v2 stays the same. Show the pressure at point 3: p3 = patm - ρg(h2+h3) - ρv2²/2g, displayed as an absolute pressure gauge with a needle. Highlight the "danger zone": if p3 < vapor pressure (~2300 Pa for water), cavitation occurs — show a flashing red warning and animated bubbles forming at the apex. Add a "comparison mode": show two siphons side by side with different h2 but same h3, confirming equal exit velocities but different apex pressures.

Widget A – Interaktivni Bernoulli explorer: tri komponente energije
Prompt:

Create an interactive HTML/JS/CSS widget that is the definitive visual explanation of Bernoulli's equation as energy conservation. Draw a pipe that the user can freely reshape: drag 5 control points on the top and bottom walls to create any pipe shape (wide, narrow, uphill, downhill sections). As the shape changes, compute and display at every cross-section along the pipe: (1) pressure head p/ρg as a blue bar growing upward from the pipe centerline, (2) velocity head v²/2g as a green bar above the blue, (3) elevation head z as a brown bar below the centerline. The tops of all three bars must always reach the same horizontal line — the total energy grade line (EGL), shown as a bold red dashed line. The hydraulic grade line (HGL = p/ρg + z) is shown as a blue dashed line. Animate flow particles moving through the pipe, speeding up in narrow sections and slowing in wide sections. Add sliders for: inlet velocity v0 (0.5–10 m/s), fluid density ρ (800–1200 kg/m³), inlet pressure p0 (50000–300000 Pa). Show a live energy audit panel listing all three heads at the user's mouse cursor position as they hover over any part of the pipe. Include a "Bernoulli quiz mode" where one component is hidden and the student must estimate it from the other two.


Widget B – Venturijeva cijev: mjerenje protoka tlakom
Prompt:

Create an interactive HTML/JS/CSS widget simulating a Venturi meter — the classic application of Bernoulli's equation for flow measurement. Draw a smooth converging-diverging pipe (Venturi shape) with a narrow throat section. Connect two vertical manometer tubes: one at the wide inlet, one at the narrow throat — the height difference Δh between the two manometer water levels is clearly visible and animated. Add sliders for: inlet diameter D1 (50–200 mm), throat diameter D2 (20–100 mm), inlet pressure p1 (100000–400000 Pa), flow velocity v1 (0.5–8 m/s), fluid density ρ. Animate: (1) flow particles accelerating through the throat and decelerating after, (2) manometer levels dropping at throat (lower pressure = higher velocity) and rising at inlet, (3) the manometer fluid columns animating smoothly as sliders change. Show the Venturi formula: V̇ = A1·A2·√(2·Δp/ρ/(A1²-A2²)) with live substituted values. Display a "measurement panel" showing: measured Δh in mm, inferred Δp in Pa, calculated V̇ in m³/s, calculated v1 and v2. Add a "prove it" animation: release a colored tracer particle and time it through the throat, confirming the calculated velocity. Show pressure distribution as a color gradient along the pipe.


Widget C – Pitotova cijev: mjerenje brzine strujanja
Prompt:

Create an interactive HTML/JS/CSS widget demonstrating a Pitot tube measuring local flow velocity using stagnation pressure. Draw a flow field (wide channel or pipe) with uniform flow moving left to right. Place a Pitot tube (small L-shaped tube with opening facing the flow) that the user can drag to different positions in the flow. At the tube opening, show the stagnation point where flow velocity drops to zero — animate streamlines diverging around the tube tip. Show two pressure taps: (1) stagnation tap at the tube nose (measures p_total = p_static + ρv²/2), (2) static tap on the side (measures p_static alone). Connect them to a differential manometer showing Δh = v²/2g — the manometer reading IS the velocity head. Add sliders for: flow velocity v (1–50 m/s), fluid density ρ (1.2 for air, 998 for water — dropdown), manometer fluid density ρ_m. Display v = √(2·Δp/ρ) = √(2g·Δh) updating live. Animate: as the user drags the Pitot tube toward the wall where velocity is lower (boundary layer), the manometer reading decreases. Show a velocity profile across the channel — the Pitot tube reads the local velocity at its position. Add a "Pitot on airplane" mode showing airspeed indicator concept.


Widget D – Torricelli: pražnjenje spremnika kroz otvor
Prompt:

Create an interactive HTML/JS/CSS widget simulating a large tank draining through a small hole at the bottom, showing how the water level and jet velocity evolve over time. Draw a tall tank with a small circular hole at the bottom right. The water level starts at H0 and drops over time. Animate the free jet as a parabolic stream of blue particles exiting horizontally and falling to the ground. The jet velocity v = cv·√(2g·h) where h is the current water level — as h drops, v decreases and the jet lands closer. Add sliders for: initial height H0 (0.5–4 m), hole diameter d (5–30 mm), tank diameter D (0.2–1.0 m), discharge coefficient cv (0.6–1.0). Show a real-time graph (scrolling time axis): blue curve = water level h(t), green curve = jet velocity v(t), red curve = landing distance x(t) — all decreasing over time following h(t) = (√H0 - cv·d²·√(2g)/(2D²)·t)². Display the current h, v, and x values as live readouts. Add a "Compare real vs ideal" toggle: cv=1.0 (blue jet, longer range) vs cv=0.62 (red jet, shorter range) simultaneously. Show the time to drain completely T_drain. Add a "Restart" button and a slow-motion toggle.


Widget E – Bernoulli i Coandă: strujanje uz zakrivljenu površinu
Prompt:

Create an interactive HTML/JS/CSS widget demonstrating the Coandă effect and curved streamline Bernoulli: a fluid jet tends to follow a curved surface because of the pressure difference that curved streamlines require. Draw a 2D flow domain with a curved convex surface (like an airfoil upper surface or a curved wall). Animate streamlines curving around the surface — they are closer together above the surface (faster, lower pressure) and more spread below (slower, higher pressure). Show pressure arrows pointing TOWARD the center of curvature at every point on the surface — this centripetal pressure gradient is what makes curved flow possible. Add a "curvature radius" slider (R = 0.5–5 m): as R decreases (tighter curve), the required pressure difference increases and the suction on the surface strengthens. Show normal pressure arrows along the curved surface whose lengths represent the magnitude of the centripetal pressure gradient dp/dn = ρv²/R. Add a velocity slider. Include an "airfoil mode" that shows a symmetric airfoil at angle of attack: streamlines curve more tightly above (shorter radius, lower pressure = lift) and less tightly below (higher pressure). Display the lift force arrow growing as angle of attack increases. Show Bernoulli equation in curved streamline form: dp/dn = ρv²/R.


Widget F – Sifon: fizika podtlaka i kavitacija
Prompt:

Create an interactive HTML/JS/CSS widget that is a comprehensive siphon physics explorer, teaching why siphons work, their limits, and cavitation. Draw an animated siphon (inverted U-tube) with fluid flowing from a left tank (high) over the apex to a right tank or open exit (low). Color-code the pressure along the entire siphon tube continuously: green = near atmospheric, yellow = moderate vacuum, orange = strong vacuum, red = cavitation danger. Add sliders for: height of apex above inlet surface h_apex (0.5–12 m), height difference between inlet and outlet h_out (0.5–6 m), fluid (water at 20°C / water at 80°C / oil — each has different vapor pressure), tube diameter d. Show a continuous pressure profile graph below the siphon: x-axis = position along tube from inlet to outlet, y-axis = absolute pressure. Mark: (1) atmospheric pressure line (solid black), (2) vapor pressure line (red dashed) — if the computed pressure curve touches this line, cavitation occurs. When cavitation is triggered, animate bubbles forming at the apex and the siphon breaking (flow stops, bubbles shown collapsing). Show the maximum theoretical siphon height = p_atm/(ρg) ≈ 10.3 m for water — display this as a horizontal limit line. Demonstrate that hot water (higher vapor pressure) has a lower practical siphon height. Add a "theoretical vs practical" comparison panel.


Widget G – Energijska i hidraulična linija gradijenta
Prompt:

Create an interactive HTML/JS/CSS widget where students build their own pipe system and watch the Energy Grade Line (EGL) and Hydraulic Grade Line (HGL) draw themselves automatically. The pipe system consists of drag-and-drop components from a palette: straight horizontal pipe, pipe going uphill, pipe going downhill, pipe narrowing (confusor), pipe widening (diffuser), pump (adds energy — EGL jumps up), valve/loss element (EGL drops). The student assembles a pipe route by dragging components into a sequence. Once assembled, the widget automatically: (1) computes velocity at each section from continuity, (2) computes pressure from Bernoulli, (3) draws the EGL (total head, bold red line) and HGL (piezometric head, blue line) above each pipe section. The gap between EGL and HGL always equals v²/2g (velocity head) — shown as a green bracket. The gap between HGL and pipe centerline equals p/ρg (pressure head) — shown as blue bracket. When HGL drops BELOW the pipe centerline, pressure is below atmospheric — show the pipe section highlighted in red (negative gauge pressure, suction). When EGL drops below the pipe, flow is impossible — trigger a warning. Add a flow rate slider and density dropdown.


Widget H – Bernoulli igra: pogodi tlak
Prompt:

Create an interactive HTML/JS/CSS game widget that teaches Bernoulli's equation through a guessing challenge. The widget shows a pipe system with flow (random geometry generated each round): varying diameters, elevations, with flow particles animated. The student sees: velocity v1, pressure p1, elevation z1 at point 1, and velocity v2, elevation z2 at point 2. The student must INPUT their estimate of p2 using a slider. When they submit, show: (1) their guess as a red horizontal line on a pressure gauge, (2) the correct answer as a green line, (3) the error in percentage. Animate the Bernoulli equation visually: show all three terms at point 1 as colored blocks stacking up to total H, then at point 2 show the blocks redistributing (velocity term changes, elevation term changes) with p2 being whatever is left to maintain the same total H. Score is kept: within 5% = perfect, within 15% = good, otherwise = try again. After each round, explain which physical effect dominated (elevation gain reduced pressure, or area change increased velocity and dropped pressure). Generate 10 progressively harder rounds: round 1 = horizontal pipe same diameter (trivial), round 10 = complex 3D path with elevation changes and diameter changes. Include a "hint" button that shows the EGL line.

