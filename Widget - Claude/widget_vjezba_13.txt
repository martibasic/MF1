Zadatak 120 – Pad tlaka u ravnoj cijevi: laminarno strujanje
Prompt:

Create an interactive HTML/JS/CSS widget for pressure drop in a straight pipe with laminar flow, demonstrating the Darcy-Weisbach equation and the Hagen-Poiseuille law. Draw a horizontal pipe (side view) with flow moving left to right. Add sliders for: pipe length L (1–50 m), pipe diameter d (0.001–0.05 m), mean velocity v (0.1–5 m/s), fluid temperature T (0–80°C — automatically updates ρ and μ for water). Compute Re = ρ·v·d/μ and display it with a color-coded indicator: GREEN (Re < 2300, laminar), YELLOW (2300–4000, transition), RED (>4000, turbulent). For laminar flow: λ = 64/Re. Compute Δp = λ·(L/d)·(ρ·v²/2) and hw = Δp/(ρ·g). Animate the parabolic velocity profile inside the pipe — dark blue at center (fast), white at walls (zero). Show the pressure as a color gradient along the pipe: dark red at inlet (high p), fading to light orange at outlet (low p). Draw two vertical pressure gauges at inlet and outlet with animated needles showing the pressure difference. Display a live physics insight: "Doubling v → Δp doubles (laminar, linear in v)" — show this by sweeping v and watching Δp. Show specific mechanical energy loss ew = Δp/ρ in J/kg. Include the full Bernoulli equation with loss term.


Zadatak 121 – Ventilacijski kanal nekružnog presjeka: ekvivalentni promjer
Prompt:

Create an interactive HTML/JS/CSS widget for airflow through a rectangular ventilation duct, demonstrating the hydraulic diameter concept and the Colebrook friction factor equation. Draw a 3D perspective view of a rectangular duct (width b, height h) with air flowing through it, shown as animated streamlines. Add sliders for: duct width b (0.05–0.5 m), duct height h (0.05–0.4 m), duct length L (2–20 m), air velocity v (1–15 m/s), roughness k (0.00001–0.001 m), temperature T (10–50°C). Compute hydraulic diameter d_ekv = 4·A/P = 4·b·h/(2(b+h)) — show this geometrically with an animated equivalent circle drawn next to the rectangle, having the same perimeter-to-area ratio. Compute Re using d_ekv. Show the Colebrook equation and solve it iteratively for λ — animate the Newton-Raphson iteration as a converging sequence on a graph (f(λ) vs λ, root-finding animation). Display λ from Colebrook, compare with Moody chart value. Compute Δp = λ·(L/d_ekv)·(ρ·v²/2) and required fan power P = V̇·Δp. Show the Moody diagram with the current operating point as a moving red dot. Display all results updating live.


Zadatak 122 – Difuzor: lokalni gubici pri proširenju
Prompt:

Create an interactive HTML/JS/CSS widget for flow through a diverging pipe (diffuser) showing local head losses due to expansion, with the loss coefficient ζ depending on the expansion angle. Draw the diffuser as a smooth expanding pipe: narrow section d1 on the left, wider section d2 on the right, half-angle α. Add sliders for: d1 (0.02–0.1 m), d2 (0.05–0.2 m, > d1), half-angle α (5–45°), inlet velocity v1 (1–15 m/s), inlet pressure p1 (50000–400000 Pa), Coriolis coefficient βc (1.0–1.1). Compute v2 from continuity (v2 = v1·A1/A2), then ideal pressure recovery from Bernoulli (no losses), then actual p2 with local loss hw_m = ζ·v1²/2g where ζ is read from a built-in table vs 2α angle. Show a local loss coefficient chart (ζ vs 2α): a U-shaped curve — minimum loss near 7° (gradual expansion), rising for both very small angles (long diffuser, friction wins) and large angles (flow separation). Draw the current α as a moving dot on this curve. Animate: flow streamlines separating from walls at large α (turbulent eddies shown), staying attached at small α. Show side-by-side: ideal pressure recovery (blue bar) vs actual (green bar), with the difference = loss (red bar). Display hw_m and p2 live.


Zadatak 123 – Cjevovod s gubitcima: geodetska visina
Prompt:

Create an interactive HTML/JS/CSS widget for a complete pipe system with multiple local losses (entrance, elbows, valve, exit), finding the required source tank elevation z1 for a given flow rate. Draw the full pipe system matching the problem: upper tank (left, elevation z1), sharp-edged entrance (ζu), first elbow (ζk), vertical downpipe height h, second elbow (ζk), valve (ζv), horizontal pipe length L, exit into lower tank (ζi, elevation z2). Add sliders for: pipe diameter d (0.02–0.1 m), pipe length L (20–150 m), flow rate V̇ (0.001–0.02 m³/s), roughness k (0.0001–0.001 m), temperature T (5–30°C), loss coefficients ζu, ζk, ζv, ζi (each 0–2.0). Compute: v = V̇/A, Re, λ from Colebrook, then total head loss hw = (Σζ + λL/d)·v²/2g. From Bernoulli: z1 = z2 + hw (both tanks open to atmosphere, v≈0). Display a LOSS BUDGET pie chart: friction loss (largest piece, orange), entrance loss, two elbow losses, valve loss, exit loss — each labeled with %. Show the EGL (sloping line) and HGL dropping at each fitting. Display z1 updating live. Add a "close valve" animation: as ζv increases, z1 must increase to maintain the same flow — dramatic effect.


Zadatak 124 – Paralelni cjevovodi: raspodjela protoka
Prompt:

Create an interactive HTML/JS/CSS widget for two parallel pipes connecting the same junction points, with a pump forcing total flow V̇_total that splits between the two branches. Draw the system: tank A (left) → pump → junction node R → two parallel pipes (pipe 1: d1, L1 on top; pipe 2: d2, L2 on bottom) → confluence node → tank B (right). Add sliders for: d1 (0.02–0.08 m), d2 (0.04–0.12 m), L1 = L2 (10–100 m), total flow V̇_total (0.01–0.05 m³/s), λ1 (0.015–0.035), λ2 (0.012–0.025), zA (2–8 m), zB (8–20 m). Key physics: head loss must be EQUAL in both parallel branches: hw1 = hw2 (same junction pressures). Solve: λ1·L1/d1·v1²/2g = λ2·L2/d2·v2²/2g with constraint V̇1 + V̇2 = V̇_total. Animate: particles flowing through both pipes simultaneously — pipe 2 (wider, less resistance) carries more flow — shown by denser particle stream and faster moving particles. Show a FLOW SPLIT bar chart: V̇1 (blue) and V̇2 (red) bars, always summing to V̇_total. Display junction pressure pR. Draw the EGL: same total head at junction for both branches, confirming equal losses. Show the conductance analogy: pipes in parallel like resistors in parallel — wider pipe has lower resistance, carries more flow.

Widget A – Interaktivni Moodyjev dijagram
Prompt:

Create an interactive HTML/JS/CSS widget that is a fully functional, zoomable Moody diagram for pipe friction factors. Draw the complete Moody chart on a log-log scale: x-axis = Reynolds number Re (10³ to 10⁸), y-axis = Darcy friction factor λ (0.008 to 0.1). Draw the following curves: (1) LAMINAR line: λ = 64/Re (straight line sloping down-left to upper-right on log-log), (2) SMOOTH PIPE curve (Prandtl-Kármán): 1/√λ = 2·log(Re·√λ) - 0.8, (3) Multiple ROUGH PIPE curves for k/D values: 0.00001, 0.0001, 0.0002, 0.0005, 0.001, 0.002, 0.005, 0.01, 0.05 — each labeled on the right axis, (4) FULLY ROUGH asymptotes (horizontal lines at high Re): 1/√λ = -2·log(k/(3.7D)). Shade three regions with different background colors: LAMINAR (Re < 2300, light green), TRANSITION (2300–4000, light yellow), TURBULENT (Re > 4000, light blue). Add input sliders for: Re (10³ to 10⁸), relative roughness k/D (0.000001 to 0.05), or alternatively material dropdown (glass, steel, cast iron, concrete, galvanized steel) that auto-sets k/D. Show the CURRENT OPERATING POINT as a large red dot that moves smoothly on the diagram as sliders change. Display λ read from the chart numerically. Add a "path" animation: as Re increases from 10³ to 10⁸ for fixed k/D, animate the dot traveling along the corresponding roughness curve — first dropping steeply (laminar), then transition bump, then gradual decrease to the fully rough horizontal asymptote. Include the Colebrook-White formula panel with live numerical substitution showing how λ is computed.


Widget B – Reynoldsov broj i tranzicija: vizualizacija strujanja
Prompt:

Create an interactive HTML/JS/CSS widget that visualizes the physical transition from laminar to turbulent pipe flow as Reynolds number increases, showing velocity profiles, streaklines, and mixing behavior. Draw a pipe cross-section (side view, long horizontal pipe). Add a single primary slider: Re (500 to 100000). At LOW Re (< 2300, laminar): animate smooth, perfectly parallel streamlines. Show the parabolic velocity profile as a filled shape — dark blue center (vmax = 2·v_mean), white edges (v=0). Show a dye streak injected at the centerline: it travels as a perfectly straight colored line without mixing. At TRANSITION Re (2300–4000): animate slightly wavy streamlines, occasional eddies appearing and disappearing. The dye streak begins to waver. At HIGH Re (> 4000, turbulent): animate chaotic, randomly fluctuating velocity vectors overlaid on the mean flow. The dye streak immediately disperses into a wide colored band — rapid mixing. Show the turbulent velocity profile: flatter top (power-law profile v/vmax = (y/R)^(1/7)), much more uniform than laminar. Display side by side: LAMINAR profile (pointy parabola) vs TURBULENT profile (flat top) for the same mean velocity. Show λ = 64/Re (laminar) vs λ from Colebrook (turbulent) — plot both on a mini Moody diagram with a moving dot. Show heat/mass transfer implications: turbulent mixing shown as colored temperature gradient rapidly equalizing across the pipe cross-section.


Widget C – Lokalni gubici: vizualni katalog armatura
Prompt:

Create an interactive HTML/JS/CSS widget that serves as an interactive catalog of pipe fittings and their local loss coefficients, allowing students to build a pipe system from components and compute total losses. Create a COMPONENT PALETTE on the left with draggable components, each with a schematic drawing: sharp entrance (ζ=0.5), re-entrant entrance (ζ=0.8), smooth bell-mouth entrance (ζ=0.04), 90° sharp elbow (ζ=0.9), 90° smooth bend (ζ=0.2), 45° elbow (ζ=0.4), gate valve fully open (ζ=0.1), gate valve 50% open (ζ=2.1), globe valve (ζ=10), sudden expansion (ζ computed), sudden contraction (ζ=0.5), gradual expansion diffuser (ζ=0.07–0.5), T-junction branch (ζ=1.0), T-junction straight (ζ=0.1), exit into tank (ζ=1.0). The user drags components into a PIPE SYSTEM canvas in the center to build a sequence. Each component shows its ζ value as a label. The TOTAL LOSS is computed as hw = (Σζ + λL/d)·v²/2g with pipe friction added for straight sections. Show each component's loss as a separate colored bar in a LOSS BUDGET chart on the right. Add sliders for: mean velocity v (0.5–10 m/s), pipe diameter d (0.02–0.2 m), fluid density ρ. Animate flow particles traveling through the assembled system — they slow slightly at each fitting (local energy loss). Show Δp = ρ·g·hw in Pa for the total system. Add a "compare" mode: show two systems side by side with different fitting choices.


Widget D – Serijski i paralelni cjevovodi: ekvivalentna cijev
Prompt:

Create an interactive HTML/JS/CSS widget demonstrating the hydraulic analogy between pipe networks and electrical circuits, with interactive series and parallel configurations. Draw TWO LAYOUTS side by side: TOP = pipe network (series/parallel pipes with flow), BOTTOM = equivalent electrical circuit (resistors in series/parallel with current). SERIES MODE: pipes connected end to end. Hydraulic resistance R_h = λ·L/(d·2g·A²) for each pipe. Total resistance = ΣR_hi. Flow rate Q is the same through all pipes. Head loss in each = R_hi·Q². Show the equivalent single pipe diameter D_eq that would give the same resistance. PARALLEL MODE: pipes between the same two nodes. Total 1/√R_total = Σ(1/√R_hi). Head loss is EQUAL in all branches. Flow splits inversely proportional to resistance. Add sliders for up to 3 pipes: L1, L2, L3, d1, d2, d3, λ1, λ2, λ3, and total flow Q_total. Animate particles: in series, all pipes carry same flow (same particle density). In parallel, wide/smooth pipes carry more flow (more particles, faster). Show the ELECTRICAL ANALOGY: highlight that voltage = head, current = flow rate, resistance = hydraulic resistance, Ohm's law = Darcy-Weisbach. Display equivalent single pipe parameters D_eq and L_eq. Show a Sankey diagram of flow distribution.


Widget E – Colebrook-White jednadžba: iterativno rješavanje
Prompt:

Create an interactive HTML/JS/CSS widget that visually demonstrates how the implicit Colebrook-White equation is solved iteratively (Newton-Raphson method), making the numerical method intuitive. Draw a large graph: x-axis = λ (0.008 to 0.1), y-axis = f(λ) = 1/√λ + 2·log(k/(3.7D) + 2.51/(Re·√λ)). The graph shows this function as a smooth curve crossing zero — the ROOT is the solution λ. Add sliders for: Re (10³ to 10⁸), k/D (0.000001 to 0.05). The function curve updates live as sliders change. ITERATION ANIMATION: Press "Solve" button to start the Newton-Raphson animation: (1) Start with initial guess λ₀ = 0.02 (shown as a vertical blue line), (2) Evaluate f(λ₀) — show the point on the curve, (3) Draw the tangent line at that point, (4) Find where tangent crosses x-axis → new λ₁, (5) Repeat — show λ₁, λ₂, λ₃ converging to the root rapidly. Animate each iteration step with a 0.5s pause, showing the zigzag path converging to the root (typically 3-5 iterations). Show a CONVERGENCE TABLE: iteration number | λ_n | f(λ_n) | error. Compare with the EXPLICIT Swamee-Jain approximation: λ ≈ 0.25/[log(k/(3.7D) + 5.74/Re^0.9)]² — show this as a horizontal dashed line on the graph, typically very close to the true root. Display final λ and percentage error of the approximation. Add "Bisection method" toggle as alternative solver — slower but guaranteed convergent.


Widget F – Hagen-Poiseuille zakon: laminarno strujanje
Prompt:

Create an interactive HTML/JS/CSS widget dedicated to the Hagen-Poiseuille law for laminar pipe flow, showing the fundamental physics of viscous resistance. Draw a pipe cross-section in 3D perspective: cylindrical pipe with the parabolic velocity profile rendered as a 3D paraboloid surface — blue at the walls, bright red at the center peak. Add sliders for: pipe radius R (0.001–0.05 m), dynamic viscosity μ (0.001 for water to 1.0 for oil, Pa·s), pressure drop Δp (100–100000 Pa), pipe length L (0.1–10 m). Compute: v(r) = (Δp/(4μL))·(R²-r²) (parabolic profile), vmax = ΔpR²/(4μL), v_mean = vmax/2, Q = π·R⁴·Δp/(8μL) (Hagen-Poiseuille). Show the 3D paraboloid profile rotating slowly so students can see its shape. Show an annular ring element at radius r with width dr — highlight that its contribution to total flow is dQ = v(r)·2π·r·dr, and animate the integration from r=0 to r=R accumulating total Q. Display the key result: Q ∝ R⁴ (fourth power!) — add a "radius sensitivity" panel: if R doubles, Q increases 16×. Show Q ∝ 1/μ: if oil (100× more viscous than water), Q reduces 100× for same Δp. Add a "viscosity temperature" slider showing how μ decreases with T for water, increasing Q. Show λ = 64/Re derived from Hagen-Poiseuille. Include a comparison chart: laminar vs turbulent Q for same Δp.


Widget G – Cjevna mreža: Hardy-Cross metoda
Prompt:

Create an interactive HTML/JS/CSS widget simulating a simple pipe network (loop network) solved by the Hardy-Cross iterative method, showing how flow distributes in a closed loop. Draw a rectangular pipe network with 4 nodes (corners) and 4 pipes forming a loop, plus 2 supply/demand pipes at selected nodes. Label nodes 1-4, pipes A-D around the loop. The user sets: supply flow at node 1 (Q_supply), demand flows at nodes 2, 3, 4 (must sum to supply), pipe diameters and lengths for each segment. The Hardy-Cross algorithm: (1) Assume initial flow in each pipe (arrows showing direction), (2) For each loop, compute head loss discrepancy ΔH = Σ(λL/D·Q²/2gA²) with signs, (3) Compute correction ΔQ = -ΔH/(2·Σ|hw/Q|), (4) Update all flows, (5) Repeat until ΔQ < tolerance. Animate EACH ITERATION: show the flow arrows updating direction and magnitude after each correction. Use arrow thickness proportional to flow rate. Use color to show head loss: red = high loss, blue = low loss. Show a CONVERGENCE graph: ΔQ vs iteration number — exponentially decaying. Display final flow rates and head losses in each pipe. Include a "flow reversal" indicator: if the assumed direction was wrong, show the arrow flipping during iteration. Add slider for total supply flow Q — as it increases, all flows scale proportionally.


Widget H – Utjecaj temperature na viskoznost i gubitke
Prompt:

Create an interactive HTML/JS/CSS widget exploring how fluid temperature affects viscosity, Reynolds number, flow regime, and pipe losses — a commonly overlooked but practically crucial effect. Draw a pipe system (side view) with a TEMPERATURE DIAL on the left (0–100°C for water, -20 to 200°C for oil). As temperature changes, show: (1) Fluid VISCOSITY μ(T) plotted on a graph — water: μ drops from 1.79 mPa·s at 0°C to 0.28 mPa·s at 100°C (dramatic decrease). Oil: μ drops even more dramatically. Animate the viscosity value on a dial-style gauge whose needle drops as temperature rises. (2) Reynolds number Re = ρ·v·D/μ INCREASES as T increases (μ decreasing) — show Re climbing and potentially crossing from laminar to turbulent. (3) Friction factor λ DECREASES as Re increases — show the operating point moving left along the Moody diagram. (4) Head loss hw = λ·L/D·v²/2g DECREASES as T increases (lower λ). Add sliders for: fluid type (water/oil/glycerin dropdown), velocity v, diameter D, pipe length L. Show a SUMMARY DASHBOARD: temperature → viscosity → Re → λ → Δp — a chain of cause-and-effect with arrows and live values. Add a PRACTICAL IMPLICATION panel: "Hot water pipes need less pumping power", "Cold oil is much harder to pump". Show Δp at T=10°C vs T=80°C as a bar comparison — dramatic difference for oil. Animate the pipe interior: at low T, slow-moving viscous fluid (darker blue); at high T, more freely flowing fluid (lighter blue, faster particles).