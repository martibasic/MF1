Zadatak 104 – Raketni potisak satelita: zakon količine gibanja
Prompt:

Create an interactive HTML/JS/CSS widget simulating a satellite in space using rocket thrust to change its trajectory. Draw a top-down view of space (black background, stars) with the satellite as a rectangular block moving horizontally. A thruster nozzle on one side ejects exhaust gas as animated bright particles flying backward. Add sliders for: satellite mass ms (1000–10000 kg), exhaust mass mi (50–300 kg), exhaust relative velocity vi (1000–5000 m/s), burn duration ti (1–5 s). Animate: (1) exhaust particles streaming backward with velocity vi relative to satellite, (2) the satellite accelerating forward — show its speed increasing on a live speedometer, (3) the trajectory curving slightly as thrust is applied. Show the momentum equation in the satellite reference frame: 0 = ms·(dvs/dt) + ṁi·vi, so dvs/dt = (ṁi·vi)/ms. Display live: ṁi = mi/ti (kg/s), acceleration as = ṁi·vi/ms (m/s²), Δv = as·ti (m/s), thrust force FP = ṁi·vi (N) in kN. Add a "control volume" toggle: draw a dashed box around the satellite+exhaust system, showing no external forces act on it (space vacuum), so total momentum is conserved. Show before/after momentum vectors. Include a "thrust comparison" panel: show FP equivalent to lifting X tons on Earth.


Zadatak 105 – Sila na prirubnici ventila
Prompt:

Create an interactive HTML/JS/CSS widget for flow through a partially closed valve (faucet/tap), where the goal is to find the total force on the flange (prirubnica) holding the valve assembly. Draw the pipe system: horizontal inlet pipe with flange on the left, the valve body in the middle (shown as an adjustable throttle opening), and the outlet pointing downward (90° bend). Add sliders for: volumetric flow rate V̇ (0.0005–0.005 m³/s), pipe diameter dc (0.01–0.04 m), gauge pressure at inlet pm (20000–150000 Pa), total system mass m (2–15 kg), momentum correction factor β (1.0–1.05). Animate: flow particles entering horizontally, curving 90° downward through the valve, exiting downward. Show all forces on the control volume as colored arrows: inlet pressure force p·A (pushing right, blue), momentum flux in (ṁ·v, blue arrow right), momentum flux out (ṁ·v, red arrow downward), gravity G = m·g (downward, brown), and reaction flange force FR (green, the unknown). Show the x and z component equations separately. Show three valve states side by side (closed/open/half-open as images) and explain how opening affects pressure. Display FR_x, FR_z, |FR| updating live.


Zadatak 106 – Konzolni cjevovod: moment savijanja
Prompt:

Create an interactive HTML/JS/CSS widget for an L-shaped pipe system fixed at the base point A: vertical pipe of height H going up from A, then horizontal pipe of length L going right, with flow exiting horizontally to the right. Add sliders for: pipe diameter dc (0.05–0.2 m), vertical height H (0.5–4 m), horizontal length L (0.5–3 m), exit velocity v0 (1–8 m/s), pipe mass per unit length mL (5–25 kg/m). Draw the L-shaped pipe fixed at A (shown as a wall socket). Show all forces as arrows: inlet momentum flux ṁ·v1 upward (at base), outlet momentum flux ṁ·v2 rightward (at end), weight of vertical section GA downward at H/2, weight of horizontal section GL downward at horizontal centroid L/2. Compute and display the bending moment at A: MA = ṁ·v2·H + GL·L/2 - ṁ·v1·0 - GA·0. Show the moment as a rotating arc arrow at A. Add a slider for L — as L increases, show the critical length where MA = 0 (momentum from flow exactly balances gravity). Animate a "moment meter" at A: a curved dial showing CW vs CCW moment. Display the zero-moment pipe length L_MA=0 updating live. Show the reaction force FR at A as well.


Zadatak 107 – Prskalica-turbina: kutni moment količine gibanja
Prompt:

Create an interactive HTML/JS/CSS widget simulating a rotating sprinkler used as a water turbine connected to a generator. Draw a top-down view of the sprinkler with 4 arms extending from a central hub, each with a nozzle at the end pointing tangentially. The sprinkler rotates counterclockwise as water exits tangentially clockwise (reaction principle). Add sliders for: flow rate V̇ (0.005–0.05 m³/s), arm radius r (0.3–1.0 m), rotation speed n (100–600 rpm → ω), nozzle diameter dm (0.005–0.02 m). Compute: water velocity exiting each nozzle in lab frame = v_jet - ω·r (relative velocity plus blade velocity), relative velocity vR = V̇/(4·A_nozzle), absolute exit velocity = vR - ω·r. Animate: (1) sprinkler rotating at speed ω with particles flying from each nozzle tangentially, (2) a generator symbol at the center with a voltage/power output indicator, (3) the reaction torque as curved arrows on the arms. Show the angular momentum equation: T = ṁ·r·vR, P = T·ω = ṁ·r·(vR - ω·r)·ω. Display P in Watts updating live. Add an "optimal speed" finder: show P vs ω curve with maximum at ω·r = vR/2. Animate the sprinkler speeding up or slowing down as V̇ slider changes.


Zadatak 108 – Vjetroturbina: snaga i sila na stup
Prompt:

Create an interactive HTML/JS/CSS widget for a large commercial wind turbine, showing power generation and tower thrust force. Draw a front-view wind turbine with rotating blades (animate them spinning), mounted on a tall tower. Show wind as animated horizontal blue arrows approaching from the left, passing through the rotor disc, and emerging slower (fewer/lighter arrows) on the right. Add sliders for: rotor diameter D (40–150 m), wind speed v1 (3–20 m/s), efficiency η (0.1–0.59, Betz limit = 0.593), air density ρ (1.1–1.4 kg/m³). Compute: ṁ = ρ·v1·(πD²/4), P_wind = ṁ·v1²/2, P_generated = η·P_wind, v2 = v1·√(1-η), FR = ṁ·(v2-v1) (negative = force on turbine in wind direction). Show a power bar chart: P_wind (total), P_generated (extracted), P_remaining (v2 kinetic energy). Display tower bending moment M = |FR|·H_tower. Show how P scales with v1³ (cubic law) — doubling wind speed = 8× power. Add a "wind speed forecast" animation: wind speed oscillates sinusoidally ±30%, showing power output varying dramatically. Include the Betz limit line at η=59.3%.


Zadatak 109 – Helikopter: uzgon, snaga i broj okretaja
Prompt:

Create an interactive HTML/JS/CSS widget simulating a helicopter hovering, showing the momentum theory of rotor lift. Draw the helicopter in side view with rotating rotor blades above. Show the control volume as a hyperbolic streamtube: wide at the top (inlet section 1, air nearly still), narrowing at the rotor disc, narrow at the bottom (outlet section 2, fast downwash). Add sliders for: helicopter mass mh (5000–20000 kg), cargo mass mt (0–20000 kg), rotor diameter D (10–25 m), rotation speed n (200–800 rpm), air density ρ (0.8–1.25 kg/m³). For UNLOADED mode: compute v2 = √(G/(ρ·A)), V̇ = v2·A, ṁ = ρ·V̇, P = ṁ·v2²/2. For LOADED mode: recalculate with total weight G = (mh+mt)·g. Show the downwash velocity v2 as a large colored arrow below the helicopter — longer and darker as load increases. Show P as a power gauge (dial indicator). Display n_loaded = n_unloaded · (v2_loaded/v2_unloaded). Add an "altitude effect" slider: as ρ decreases (high altitude), show v2 must increase (faster rotor) to maintain same lift. Animate rotor blades spinning at speed proportional to n. Show a before/after comparison: unloaded vs loaded power and rotor speed.


Zadatak 110 – Brana s razlikom razina: sila i brzine
Prompt:

Create an interactive HTML/JS/CSS widget for flow under a sluice gate (brana) where water level h1 on the upstream side is higher than h2 on the downstream side, generating a force on the gate. Draw a horizontal channel cross-section with the gate as a vertical blue rectangle. Water level h1 (high, left) and h2 (low, right) shown as animated blue fills. Add sliders for: upstream depth h1 (0.5–4 m), downstream depth h2 (0.1–2 m, always < h1), channel width b (0.5–5 m). Apply Bernoulli (free surface to free surface) + continuity: h1·v1 = h2·v2, then v1²/2g + h1 = v2²/2g + h2 → solve for v1 and v2. Animate: upstream particles moving slowly left to right (v1 small), squeezing through the gate gap and accelerating (v2 large). Show the hydrostatic pressure distributions on each side of the gate as triangular diagrams: left (blue, high triangle h1) and right (red, smaller triangle h2) — the difference in the two triangles equals the net force on the gate. Display F = ½·ρ·g·b·(h1²-h2²) - ρ·b·h2·v2² + ρ·b·h1·v1² (full momentum equation) updating live. Show v1, v2, F, Q = b·h1·v1 all live.


Zadatak 111 – Pelton turbina: kutni moment i snaga
Prompt:

Create an interactive HTML/JS/CSS widget for a Pelton wheel turbine, showing the velocity triangle analysis and power output. Draw the Pelton wheel as a circle with curved buckets (lopatice) around the rim. A high-velocity water jet (v) enters from the left, hitting one bucket at a time. Show the velocity triangle for a single bucket: absolute jet velocity v (horizontal, long blue arrow), bucket velocity ωr (horizontal, shorter green arrow), relative velocity vr = v - ωr (orange arrow). After deflection through angle β, show the exit velocity triangle. Add sliders for: jet velocity v (20–80 m/s), rotation speed n (50–300 rpm → ω), wheel radius r (0.5–3 m), flow rate V̇ (2–20 m³/s), deflection angle β (90–175°). Compute: vl = ω·r, vr = v - vl, torque M = r·ρ·V̇·vr·(1-cosβ), power P = M·ω. Show a POWER vs SPEED graph: P as a function of ω — it is a parabola peaking at ω·r = v/2 (optimal speed). Draw the current operating point as a moving dot on this curve. Animate: bucket passing through the jet, water deflecting through angle β with animated particles curving inside the bucket. Show "ideal conditions": cosβ=-1 (β=180°) gives maximum force. Display M and P updating live in kW and MW.


Zadatak 112 – Flyboard: sila mlaza i nestacionarno gibanje
Prompt:

Create an interactive HTML/JS/CSS widget simulating a person rising on a Flyboard powered by 4 water jets pointing downward. Draw a side view: person standing on the board, 4 downward-pointing nozzles, water jets shooting downward as animated blue streams. Show the ground below and a height marker on the left. Add sliders for: nozzle diameter ds (0.03–0.08 m), jet velocity vm (10–25 m/s), total mass m (100–200 kg), target height h (5–20 m). Compute: ṁ = ρ·4·(πds²/4)·vm, thrust F = ṁ·vm, net upward force = F - G = F - mg, acceleration a = (F-G)/m. Show a minimum velocity indicator: vmin = √(G/(ρ·4·πds²/4)) — when vm < vmin, the person stays on the ground (board sinks into water). Animate the person rising with constant acceleration a: position h(t) = ½·a·t². Show a live height meter on the left filling up. When the jets turn off at h=10m, animate the person coasting upward then decelerating under gravity, reaching hmax = h + vh²/(2g) extra height. Show time above h=10m as Δt = 2·vh/g. Display: vmin, a, time to reach h, velocity at h, hmax, Δt all updating live. Add a "launch" button that runs the full animation sequence automatically.

Naravno! Evo dodatnih widgeta temeljenih na fizikalnim principima Vježbe 12:

Widget A – Zakon očuvanja količine gibanja: sudari i eksplozije
Prompt:

Create an interactive HTML/JS/CSS widget demonstrating conservation of linear momentum through collisions and explosions in a fluid context. Draw a horizontal track (frictionless surface in space) with two objects that can collide. Add a MODE toggle: (1) ELASTIC COLLISION: two masses approach each other, collide, bounce back — total momentum conserved, kinetic energy conserved. (2) INELASTIC COLLISION: masses stick together after impact — momentum conserved, kinetic energy reduced. (3) EXPLOSION: one stationary mass splits into two pieces flying apart — like a rocket ejecting mass. Add sliders for: mass 1 m1 (100–5000 kg), mass 2 m2 (100–5000 kg), initial velocity v1 (-20 to +20 m/s), initial velocity v2 (-20 to +20 m/s). For each mode, animate the collision/explosion with colored particles. Show momentum vectors as horizontal arrows for each object: blue before, red after. Show a MOMENTUM SCOREBOARD: p_total before (left bar) and p_total after (right bar) — they must always be equal, shown as equal-height green bars. Show kinetic energy before and after — only equal for elastic. Add a "fluid jet" analogy panel: show how rocket exhaust is equivalent to an explosion in reverse. Display all velocities and momenta numerically live.


Widget B – Kontrolni volumen u gibanju: relativno strujanje
Prompt:

Create an interactive HTML/JS/CSS widget that demonstrates the concept of a moving control volume (KV) — the fundamental idea used for satellites, rockets, and moving blades. Draw a 2D scene with a moving object (rocket/boat/blade) traveling to the right at velocity U. Show TWO reference frames simultaneously side by side: LEFT = LABORATORY frame (stationary observer) — the object moves right, exhaust moves left at absolute velocity. RIGHT = MOVING frame (observer riding with the object) — the object appears stationary, exhaust flies backward at relative velocity vi. Add sliders for: object velocity U (0–500 m/s), exhaust relative velocity vi (500–5000 m/s), mass flow rate ṁ (5–100 kg/s). In BOTH frames, show animated particles with velocity arrows — the physics (force, acceleration) must be the same in both frames. Show the Galilean transformation: v_lab = v_relative + U for each particle. Display the thrust force F = ṁ·vi — it is the SAME in both frames (force is frame-independent). Add a "transform" animation button: smoothly transition between the two frames, showing particles changing velocity but force staying constant. Show the momentum equation in each frame side by side. Include a "why moving frame?" explanation panel.


Widget C – Raketna jednadžba: Tsiolkovsky
Prompt:

Create an interactive HTML/JS/CSS widget for the Tsiolkovsky rocket equation: Δv = vi·ln(m0/mf), showing how a rocket's velocity change depends on exhaust velocity and mass ratio. Draw a rocket in space traveling from left to right. As it burns fuel, it gets visually smaller (fuel mass depleted). The exhaust trail behind it grows longer and denser. Add sliders for: initial mass m0 (1000–100000 kg), final mass mf (100–50000 kg, always < m0), exhaust velocity vi (1000–5000 m/s). Show the rocket getting lighter in real time as fuel burns — animate fuel tank visually emptying. Display Δv = vi·ln(m0/mf) updating live. Show a graph: Δv vs mass ratio m0/mf — it is a logarithmic curve. Mark: current operating point as a movable dot. Show the "mass ratio" m0/mf as a large dial — typical rockets need ratios of 7–25. Add comparison markers on the graph: orbit requires Δv ≈ 7800 m/s, Moon requires ≈ 3200 m/s extra — show whether the current rocket can achieve these. Display: fuel consumed = m0 - mf, burn time = (m0-mf)/ṁ, average thrust = ṁ·vi. Add a "staging" mode: split the rocket into 2 stages and show how staging dramatically improves Δv for the same total mass ratio. Animate the first stage detaching and the second stage igniting.


Widget D – Pelton vs. Francis vs. Kaplan: usporedba hidroelektrana
Prompt:

Create an interactive HTML/JS/CSS widget comparing three types of hydraulic turbines — Pelton (impulse), Francis (mixed flow), and Kaplan (axial propeller) — showing when each is appropriate based on head and flow rate. Draw all three turbine types in schematic cross-section, side by side. For each, animate water flowing through with colored particles: Pelton — high-speed jet hitting cups (high head, low flow), Francis — spiral casing with guide vanes and runner (medium head/flow), Kaplan — axial propeller in a tube (low head, high flow). Add sliders for: available head H (1–1000 m), flow rate Q (0.1–1000 m³/s), efficiency η (0.80–0.95). Compute: P = η·ρ·g·Q·H for each turbine type. Show a HEAD vs FLOW RATE application chart (like a Moody diagram) with regions colored: PELTON region (high H, low Q — top left), FRANCIS region (middle), KAPLAN region (low H, high Q — bottom right). Plot the current operating point as a movable dot — highlight which turbine type is optimal. Show specific speed Ns = n·√Q/H^(3/4) for each type. Animate the runner rotation speed: Pelton rotates slowly (large diameter, low RPM), Kaplan rotates fast (small diameter, high RPM). Display P in MW for all three types simultaneously.


Widget E – Moment količine gibanja: upravljanje kutnim momentom
Prompt:

Create an interactive HTML/JS/CSS widget demonstrating angular momentum conservation in rotating fluid systems — the rotational equivalent of linear momentum. Draw a rotating platform (top view) with fluid flowing through radial channels from center (r=0) to rim (r=R). No external torques — angular momentum is conserved. Show: as fluid moves outward (r increases), angular velocity ω decreases to conserve L = I·ω = ṁ·r·vθ. Add sliders for: inlet radius r1 (0.05–0.3 m), outlet radius r2 (0.2–1.0 m), inlet tangential velocity vθ1 (1–10 m/s), mass flow rate ṁ (1–20 kg/s). Animate particles spiraling outward from r1 to r2 — they slow their tangential speed as r increases. Show velocity vectors at multiple radii: long arrows near center (fast), shorter arrows at rim (slow). Display the Euler turbomachine equation: T = ṁ·(r2·vθ2 - r1·vθ1). For a TURBINE (extracting torque): T > 0, fluid loses angular momentum. For a PUMP (adding torque): T < 0, fluid gains angular momentum. Add a "figure skater" analogy: show a skater pulling arms in (r decreases) → ω increases — same physics. Show angular momentum L = ṁ·r·vθ as a bar at each radius, always constant (for free rotation). Display T and P = T·ω live.


Widget F – Sila mlaza na lopaticu: stacionarna i gibajuća
Prompt:

Create an interactive HTML/JS/CSS widget that systematically compares jet force on a stationary vane versus a moving vane, building intuition for turbine efficiency. Draw a nozzle on the left shooting a water jet horizontally at velocity v. The vane (curved bucket) can be set to STATIONARY or MOVING (velocity u to the right). For STATIONARY vane: relative velocity = v, force F = ṁ·v·(1-cosβ), power = 0 (vane doesn't move). For MOVING vane: relative velocity = v-u, only (v-u) is the effective jet velocity, force F = ṁ·(v-u)·(1-cosβ), power P = F·u = ṁ·u·(v-u)·(1-cosβ). Add sliders for: jet velocity v (10–50 m/s), vane velocity u (0 to v, m/s), deflection angle β (90–180°), ṁ (1–20 kg/s). Show a POWER vs VANE SPEED graph: P(u) = ṁ·u·(v-u)·(1-cosβ) — a parabola peaking at u = v/2. Draw the current point as a movable dot. Highlight: at u=0 (stationary vane), maximum FORCE but zero POWER. At u=v (vane runs away from jet), zero force and zero power. At u=v/2 (OPTIMAL): maximum POWER. Show efficiency η = P/(½·ṁ·v²) peaking at 50% for flat plate (β=90°) and approaching higher values for β→180°. Animate particles entering and exiting the vane with velocity arrows in both lab and vane frames.


Widget G – Nestacionarno gibanje tijela: raketa i Flyboard simulacija
Prompt:

Create an interactive HTML/JS/CSS widget that numerically integrates Newton's second law for a body propelled by a fluid jet, showing the full motion trajectory over time. Draw a 2D canvas (x-z plane): ground at z=0, the propelled body (rocket/flyboard/satellite) as a small icon. Animate the body moving in real time according to: a(t) = (F_thrust(t) - G - F_drag(t)) / m, v(t) = ∫a dt, z(t) = ∫v dt. Add sliders for: body mass m (50–5000 kg), jet velocity vjet (10–5000 m/s), nozzle area A (0.001–0.1 m²), fluid density ρ (1.2 for air, 1000 for water), burn duration t_burn (1–30 s), drag coefficient Cd (0–1). Thrust = ρ·A·vjet². Show three PHASES with different colors on the trajectory: POWERED phase (thrust > gravity, accelerating upward — green trail), COASTING phase (thrust off, decelerating under gravity — yellow trail), FALLING phase (gravity pulls down — red trail). Show a real-time graph on the right: height z(t), velocity v(t), acceleration a(t) — all three curves drawing themselves as the simulation runs. Mark: max height, time of max height, time of impact. Add a FORCE BALANCE dial: when thrust = weight, dial is centered (hover condition). Add buttons: Play, Pause, Reset, and a "Tsiolkovsky mode" where mass decreases as fuel burns.


Widget H – Helikopter vs. Vjetroturbina: obrnuta fizika
Prompt:

Create an interactive HTML/JS/CSS widget that shows the beautiful symmetry between a helicopter (pump — adds energy to fluid to create thrust) and a wind turbine (turbine — extracts energy from fluid momentum). Draw both machines side by side. HELICOPTER (left): rotor accelerates stationary air downward, creating upward thrust. WIND TURBINE (right): incoming wind pushes on rotor, extracting kinetic energy and slowing the wind. Add sliders for: rotor/blade diameter D (5–20 m), air velocity v_wind (for turbine, 3–20 m/s), rotation speed n (100–600 rpm), air density ρ. For HELICOPTER: show quiet air above (v1≈0), rotor disc, accelerated downwash below (v2). Force = ṁ·v2 = weight. P = ṁ·v2²/2. For WIND TURBINE: show fast wind approaching (v1), slower wind leaving (v2). P = η·½·ṁ·v1². Show the SAME equations apply but with reversed energy flow: helicopter PUTS energy IN, turbine TAKES energy OUT. Draw streamtubes: helicopter streamtube NARROWS going down (flow accelerates), turbine streamtube WIDENS going downstream (flow decelerates). Show the Betz limit for turbine and the equivalent minimum power for helicopter. Add a "scale comparison": what diameter turbine generates the same power a helicopter consumes? — dramatically illustrates energy scales. Display both powers in kW live.